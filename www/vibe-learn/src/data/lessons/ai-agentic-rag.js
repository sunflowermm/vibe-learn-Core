/** Agentic RAG */
export default `# Agentic RAG

> **时间线**：经典「一检索一生成」RAG 在多跳问题上不够用之后，出现 **Agentic RAG**：  
> 由 Agent **决定何时检索、检索什么、是否再检索、如何校验**。

## 为什么诞生

| 痛点（经典 RAG） | 说明 |
|------|------|
| **一次 Top-K 不够** | 问题要跨多篇、多跳推理 |
| **问法差检索就崩** | 用户口语 ≠ 文档标题 |
| **噪声片段污染** | 检回无关块，模型被带偏 |
| **不会「再说一次」** | 流水线缺反馈环 |

于是把检索升级成工具：模型可以改写查询、换源、对比矛盾证据，再回答。

---

## 它有什么作用

| 能力 | 作用 |
|------|------|
| **多轮检索** | 先找 A，再根据 A 去找 B |
| **查询改写** | 把用户话翻译成更好的检索词 |
| **工具化检索** | 检索 = 一种 tool；与网页搜索、MCP 同类 |
| **自我校验** | 证据不足就继续搜，而不是硬编 |

\`\`\`mermaid
flowchart LR
  Q[问题] --> A[Agent]
  A -->|tool| R[检索]
  R --> A
  A -->|不够| R
  A --> Ans[回答]
\`\`\`

---

## 与经典 RAG / 纯 Agent 的差别

| | 经典 RAG | Agentic RAG |
|--|----------|-------------|
| 检索次数 | 通常一次 | 可多次、可分支 |
| 控制者 | 固定管道 | 模型（+规则）动态决定 |
| 复杂度 | 低 | 高（成本、延迟、可观测性） |

## 接到下一站

长上下文、工具直接读文件、成本账本出现后——行业开始 **重新评估「默认上 RAG」**。下一课讲这场 **观念转变**。
`;

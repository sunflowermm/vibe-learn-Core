/** Agent CLI */
export default `# Agent CLI

> **时间线**：终端本就是工程师主场；模型够用后，出现 **在 CLI 里对话并改仓库** 的 Agent（Cursor CLI、Claude Code、各类 coding agent）。  
> 本课区分 **传统 CLI** 与 **Agent CLI**。

## 时间线上的位置

上一课：IDE 里的子代理分工。  
本课：同一套「模型 + 工具 + 规则」换到 **终端壳**。  
下一课：跨工具都能读的项目说明书 **AGENTS.md**。

---

## 为什么诞生

| 痛点 | 说明 |
|------|------|
| **SSH / 无 GUI** | 服务器上没法开完整 IDE，仍要改代码、跑检查 |
| **脚本与 CI 亲和** | 终端天然可管道、可无人值守 |
| **工程师肌肉记忆** | 大量工作本来就在 shell 里 |
| **远程与自动化** | 需要无头模式跑 Agent 任务 |

不是「淘汰 IDE」，而是 **多一个运行面**：能聊、能跑命令、能改文件。

---

## 它有什么作用

| 作用 | 人话 |
|------|------|
| **在仓库根对话施工** | 读文件、跑 \`pnpm\`/\`git\`、提交前检查 |
| **复用同一操作面** | Rules / Skills / MCP 思路与 IDE Agent 同源 |
| **适合运维向任务** | 日志、部署脚本、远程环境 |
| **可脚本化** | 把 Agent 嵌进自动化流水线（需强约束） |

| | 传统 CLI | Agent CLI |
|--|----------|-----------|
| 输入 | 精确命令 | 自然语言 + \`/技能\` |
| 谁决定下一步 | 你 | 模型规划 + 你的规则 |
| 风险 | 敲错命令 | 模型可能乱执行 → 靠 Rules/确认 |

---

## 深入浅出

传统 CLI：你是驾驶员，每个挡位自己挂。  
Agent CLI：你说目的地，副驾（模型）接手换挡——但油门限位器（Rules）和导航手册（Skills）仍由你提供。

第一章学的 \`pwd\`/\`ls\` 不会过时；Agent 往往 **正是去调用这些命令**。

## 接到下一站

IDE 与 CLI 都要读懂「这是什么项目」——下一课 **AGENTS.md**。
`;

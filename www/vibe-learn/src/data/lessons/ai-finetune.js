/** 模型微调 */
export default `# 模型微调

> **时间线**：预训练基座出现后，**微调（Fine-tuning）** 成为「变成可用产品」的常规步骤。  
> 本课讲清：为何要微调、常见种类、和「只写提示」的差别。

## 为什么诞生

| 痛点 | 说明 |
|------|------|
| **基座只会续写** | 海量文本预训练 ≠ 听指令、守格式、拒危险请求 |
| **领域口吻不对** | 医疗/法律/本仓库术语需要专项数据 |
| **提示堆叠有上限** | 每次塞超长说明书又贵又脆 |

于是：在基座上用 **较小、较贵精的数据** 再训练一截，得到 Chat/Instruct/领域模型。

---

## 常见类型与作用

| 类型 | 作用（人话） |
|------|----------------|
| **SFT（监督微调）** | 示范「问→答」对，教会格式与任务 |
| **偏好对齐（RLHF / DPO 等）** | 按人类偏好排序，减少胡话、提高听劝 |
| **领域继续预训练 / 微调** | 灌入行业语料，提高术语正确率 |
| **参数高效微调（LoRA 等）** | 少改参数就能适配，省显存与成本 |
| **偏好/工具专项微调** | 更稳地产出 tool_calls 等结构化行为 |

\`\`\`mermaid
flowchart LR
  Base[基座预训练] --> SFT[SFT]
  SFT --> Align[对齐]
  Align --> Prod[对话 / 工具模型]
  Base --> LoRA[LoRA 等领域适配]
\`\`\`

---

## 和提示工程的边界

| | 提示（Prompt） | 微调 |
|--|----------------|------|
| 改什么 | 当次输入 | 模型权重（或适配器） |
| 成本 | 每次调用都付上下文 | 训练一次性，推理可更短 |
| 适合 | 快速试错、个性化当场说明 | 稳定风格、领域、结构化行为 |

---

## 深入浅出

基座 = 读过图书馆的人。  
微调 = 上岗培训 + 公司规章考试。  
LoRA = 不换整脑，只夹一本薄薄的岗位便签。

## 接到下一站

微调出「会聊天的模型」后，还要做成产品——下一课 **对话产品化**。
`;

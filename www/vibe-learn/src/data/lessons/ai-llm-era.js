/** 从规则到大模型 */
export default `# 从规则到大模型

> **时间线中段**：AI 有了名字之后，方法换了好几代。  
> 本课讲清一条主线：**手写规则不够用 → 机器学习 → 深度学习 → 大语言模型（LLM）**。

## 时间线上的位置

| 大致阶段 | 做法直觉 |
|----------|----------|
| 符号 / 专家系统 | 专家把规则写成 if-then |
| 统计机器学习 | 从标注数据里拟合规律 |
| 深度学习 | 很深的神经网络 + 大数据 + GPU |
| **大语言模型** | 用海量文本做「预测下一个词」，涌现出通用语言能力 |

上一课：学科诞生。下一课：这种能力如何变成 **人人会用的对话产品**。

---

## 为什么诞生（每一跳的痛点）

\`\`\`mermaid
flowchart TB
  R[规则系统] -->|规则写不完、遇新情况就崩| ML[机器学习]
  ML -->|特征工程累、复杂感知难| DL[深度学习]
  DL -->|专用模型太多、语言任务要统一底座| LLM[大语言模型]
\`\`\`

| 阶段 | 诞生原因（痛点） |
|------|------------------|
| **规则系统** | 早期算力弱，先靠人把知识写清楚 |
| **机器学习** | 现实世界组合爆炸，规则永远写不全 |
| **深度学习** | 手工特征不够；需要端到端从原始信号学表示 |
| **LLM** | 每个任务训一个小模型成本高；需要 **一个通用语言底座** 再适配下游 |

---

## 它有什么作用

| 概念 | 作用 |
|------|------|
| **机器学习** | 让程序「看例子长本事」，而不是只执行死规则 |
| **深度学习** | 把感知类任务（图像、语音）做到可用 |
| **LLM** | 把「懂语言、会生成、能跟指令」收成可调用的能力模块 |

对工程的意义：从此你可以像调数据库一样 **调一个「会说话的函数」**——但仍不会自动连网、不会自动改你的仓库；那是后面 **工具调用 / MCP / Agent** 的故事。

---

## 深入浅出

把 LLM 想成：读过海量文本的「超级自动补全」。  
它擅长 **续写与改写语言**；对真实世界的「动手」要另接管道。

| 易混 | 澄清 |
|------|------|
| AI | 总目标 / 学科 |
| ML | 用数据学习的一大类方法 |
| LLM | 当前最火的一种能力形态 |

## 接到下一站

有了「会学习的模型」之后，先分清种类——下一课 **模型类型**。
`;

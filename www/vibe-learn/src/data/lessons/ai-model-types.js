/** 模型类型 */
export default `# 模型类型

> **时间线位置**：有了「从数据学习」之后，工程上首先要分清 **你在用哪一类模型**。  
> 本课只做分类地图；下一课讲架构（Transformer 等）。

## 为什么需要「分类型」

口头说「上个 AI」什么都没说清。选型、报价、延迟、能不能微调，都取决于类型。

---

## 常见切法（深入浅出）

### 1. 按任务形态

| 类型 | 作用 | 例子直觉 |
|------|------|----------|
| **判别 / 分类** | 判断属于哪一类 | 垃圾邮件、情感正负 |
| **生成** | 写出新内容 | 文案、代码、图像 |
| **嵌入（Embedding）** | 把文本变成向量，便于检索相似 | RAG 里的「索引器」 |
| **多模态** | 同时吃图/文/音 | 看图回答、语音助手 |

### 2. 按「语言」角色

| 类型 | 作用 |
|------|------|
| **基座语言模型（Base / Pretrained）** | 海量文本预训练，擅长续写，未必听话 |
| **对话 / 指令模型（Chat / Instruct）** | 再经微调，听人话、会拒答 |
| **推理加强模型** | 更擅长多步思考（各家命名不同） |
| **编码专用 / 补全模型** | 偏向 IDE 补全、填空 |

### 3. 按部署与规模

| 类型 | 诞生原因 | 作用 |
|------|----------|------|
| **云端大模型 API** | 训练贵，集中服务 | 开箱即用 |
| **开源可自托管** | 要数据主权 / 成本可控 | 本地或私有云跑 |
| **小模型 / 端侧** | 手机、边缘要离线低延迟 | 牺牲上限换可控 |

\`\`\`mermaid
flowchart TB
  AI[说「模型」] --> Task[按任务]
  AI --> Role[按语言角色]
  AI --> Deploy[按部署]
\`\`\`

---

## 它有什么作用

- 让你和同事用 **同一张地图** 对话，避免「我们上了 AI」这种空话  
- 为后面 **微调、RAG、Agent** 选对底座：嵌入模型 ≠ 聊天模型  

## 接到下一站

类型清楚后，还要问：**身子骨是什么架构？**——下一课先打破迷信：**不止 Transformer**。
`;

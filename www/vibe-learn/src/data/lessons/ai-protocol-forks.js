/** 协议分层与自有协议 */
export default `# 协议分层与自有协议

> **时间线：2024–2026**：Chat Completions 普及之后，并没有「只剩一套协议」。  
> 各厂商与基金会在 **不同层** 继续发明方言——这不是单纯撕破脸，而是 **问题变复杂了**。

## 时间线上的位置

上一课：MCP 统一「接工具」。  
本课：看清 **三层协议** 各自为何存在。  
下一课起：编程 Agent 的操作面（Rules → Skills → …）。

---

## 为什么诞生

兼容层 **便宜**；完整能力与新场景 **贵**。

| 新问题 | 旧 Chat Completions 不够的地方 |
|--------|--------------------------------|
| 长思考、提示缓存、特殊块 | 压缩进兼容层会丢特性 |
| Agent 长循环、内置工具、服务端状态 | 需要更偏「运行时」的 API（如 Responses） |
| 多个 Agent 互相委派 | 需要横向协作协议（A2A / ACP 等） |

于是出现：**兼容人人有，原生细活各家做；工具用 MCP；协作另开一层。**

---

## 它有什么作用

\`\`\`mermaid
flowchart TB
  subgraph L1["L1 推理面"]
    CC[Chat Completions 兼容]
    MSG[Messages / Responses 等原生]
  end
  subgraph L2["L2 工具面"]
    MCP[MCP]
  end
  subgraph L3["L3 协作面"]
    A2A[A2A / ACP…]
  end
  L1 --> L2
  L2 --> L3
\`\`\`

| 层 | 作用 |
|----|------|
| **L1 兼容** | 最广迁移面；本仓默认心智模型 |
| **L1 原生** | 吃满某一家模型的独特能力 |
| **L2 MCP** | 外接工具与数据的标准 |
| **L3 A2A 等** | Agent 发现彼此、委派任务、跟踪生命周期 |

---

## 深入浅出

别问「哪套协议会赢掉其它所有」——先问 **它管的是哪一层**。  

- 电源插头（兼容 Chat）  
- 品牌原装配件（Messages / Responses）  
- USB 外设（MCP）  
- 团队对讲机（A2A）  

四样可以同时存在于一个系统里。

## 接到下一站

协议解决「机器怎么连」；编程助手还要解决「人怎么驯服 Agent」——下一课 **Rules（规则）**。
`;

/** RAG 观念转变 */
export default `# RAG 观念转变

> **时间线：约 2024–2026**：长上下文窗口变大、Agent 可直接用工具读文件/搜网之后，  
> 社区出现「还要不要默认上 RAG？」的讨论。  
> **结论不是「RAG 已死」**，而是：**别再把「向量库流水线」当成唯一教条**。

## 为什么会出现「不那么倡导 RAG」的声音

| 新条件 | 冲击 |
|--------|------|
| **超长上下文 + 缓存** | 小而稳的知识库，整段塞进提示有时更简单 |
| **Agent + 工具** | 直接 \`read_file\` / 搜索，未必先建向量库 |
| **运维成本暴露** | 切块、嵌入、脏数据、权限、评测，比 demo 贵得多 |
| **「为 RAG 而 RAG」** | 文档很少仍硬上向量库，得不偿失 |

口号从「凡 AI 必 RAG」转向 **Context Engineering（上下文工程）**：先问模型到底需要看见什么。

---

## 转变后，RAG 还剩什么作用

| 仍强依赖检索的场景 | 原因 |
|--------------------|------|
| **超大、常变语料** | 不可能每次塞进窗口 |
| **权限 / 审计 / 引用** | 检索层是控制面，不只是省 token |
| **高 QPS 成本账** | 全量长上下文账单可爆炸 |
| **多租户隔离** | 先过滤可见文档再生成 |

业界常见收敛：**混合**——检索缩小候选，再交给长上下文做推理；或按查询路由：有的走长上下文，有的走 RAG，有的走 Agent 工具。

\`\`\`mermaid
flowchart TB
  Q[查询] --> Router{路由}
  Router -->|小而稳| LC[长上下文]
  Router -->|大而变 / 要引用| RAG[检索增强]
  Router -->|多跳办事| Agent[工具 / Agentic]
\`\`\`

---

## 深入浅出

以前：每人发一本影印笔记（向量库），考试只准翻。  
现在：有人书桌够大（长上下文），有人直接去书架搬（工具读文件），影印笔记仍适合图书馆级别的馆藏。

**不要背「RAG 已死」；要会判断何时用、何时不用、何时与 Agent/长上下文组合。**

## 接到下一站

工具互操作需要标准插头——下一课 **MCP**（若你刚学完 RAG 观念转变，也是接到这里）。
`;

/** RAG */
export default `# RAG · 检索增强生成

> **时间线：约 2020 前后概念成型，2023 起随 LLM 应用爆发而普及。**  
> **RAG（Retrieval-Augmented Generation）**：先 **检索** 相关资料，再把片段塞进提示，让模型 **基于证据生成**。

## 为什么诞生

| 痛点 | 说明 |
|------|------|
| **模型知识截止** | 训练后世界还在变；私有文档根本没进权重 |
| **幻觉** | 凭参数「编」答案，看起来像真的 |
| **微调太重** | 每改一版知识就全量/频繁微调不划算 |
| **要可引用** | 企业要「答案从哪段来」 |

思路：别把整个知识库烤进模型；**用时去书架抽几页**。

---

## 它有什么作用

| 步骤 | 作用 |
|------|------|
| **切块 / 索引** | 把文档变成可搜的单元（常配合向量库） |
| **检索** | 按问题找出 Top-K 片段 |
| **增强生成** | 片段 + 问题 → 模型作答 |
| **引用** | 可回指来源，便于人工核对 |

\`\`\`mermaid
sequenceDiagram
  participant U as 用户
  participant R as 检索
  participant I as 索引
  participant M as 模型
  U->>R: 问题
  R->>I: 相似搜索
  I-->>R: 相关片段
  R->>M: 问题 + 片段
  M-->>U: 带依据的回答
\`\`\`

---

## 深入浅出

开卷考试：允许看笔记，但笔记要自己先整理好（索引），考试时只能翻最相关的几页（检索），再写答卷（生成）。

本仓 AiWorkflow 的「记忆 / 知识增强」属于同一家族思路。

## 接到下一站

单次检索不够聪明时——下一课 **Agentic RAG**：把检索变成可多轮的 Agent 行为。
`;

/** Rules */
export default `# Rules · 规则

> **时间线：编程 Agent 普及期（约 2023–）** 起被重度使用；形态从单文件 \`.cursorrules\` 演进到 **按目录/glob 生效的规则集**。  
> Rules = 写给 Agent 的 **硬约束与项目宪法**，不是长篇教程。

## 时间线上的位置

上一课：协议解决机器互联。  
本课：人开始给 Agent **常驻规矩**。  
下一课：规矩太长会撑爆上下文 → **Skills 按需加载** 登场。

---

## 为什么诞生

| 痛点 | 说明 |
|------|------|
| **每次重说一遍** | 「本仓只用 pnpm」「别改 src/」复制粘贴到每个对话 |
| **模型爱发挥** | 无约束时乱加依赖、乱重构、越权改文件 |
| **团队要一致** | 多人/多会话需要同一套底线 |

需要一种 **进仓库就能自动生效** 的说明书片段——短、硬、可匹配文件。

---

## 它有什么作用

| 作用 | 人话 |
|------|------|
| **设定红线** | 安全、目录边界、包管理契约 |
| **统一风格** | 命名、响应格式、禁止旧写法 |
| **按范围触发** | \`alwaysApply\` 或 \`globs\` 只在相关文件时注入 |
| **省口舌** | 新开对话不必从零交代项目忌讳 |

典型落点：\`.cursor/rules/*.mdc\`。

---

## 深入浅出

| Rules 适合 | Rules 不适合 |
|------------|----------------|
| 「禁止 force push main」 | 三千字部署手册 |
| 「Core 业务别进 src/」 | 偶尔才做的发布 checklist |
| 短编码规范 | 带脚本的完整流程包 |

> **规则是护栏；流程是导航。** 护栏一直在；导航只在上路时打开——那是 Skills。

## 接到下一站

当「有时才用的长流程」塞进 Rules 导致上下文爆炸——下一课 **Skills**。
`;

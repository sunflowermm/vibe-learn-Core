/** Transformer */
export default `# Transformer

> **时间线：2017**，论文 *Attention Is All You Need* 提出 Transformer。  
> 后来多数现代 **大语言模型** 的主干都长在它（或其变体）上。

## 为什么诞生

| 痛点（相对 RNN 时代） | 说明 |
|------|------|
| **难并行** | RNN 一步依赖上一步，GPU 吃不饱 |
| **长距离依赖弱** | 隔得很远的词，信号容易淡 |
| **机器翻译要更强编码器-解码器** | 需要更好的「对齐」机制 |

核心想法：用 **自注意力（self-attention）** 让序列中任意位置直接互相看，并把计算组织成可并行的矩阵运算。

---

## 它有什么作用

| 作用 | 人话 |
|------|------|
| **并行训练** | 同样数据量下，更能吃满算力 |
| **长程关系** | 「代词指谁」这类跨越更远 |
| **可堆叠** | 堆很多层 → 大模型的「层数」故事 |
| **可迁移** | 从翻译扩到语言建模、多模态（改输入输出） |

对你的工程意义：调用的 GPT/Claude/开源 Llama 系，口语上常是 **「Transformer 语言模型」**；微调、量化、推理优化都默认围绕这套结构谈。

\`\`\`mermaid
flowchart TB
  In[词 / 位置编码] --> Attn[自注意力]
  Attn --> FFN[前馈层]
  FFN --> Stack[堆多层]
  Stack --> Out[下一个词概率等]
\`\`\`

---

## 深入浅出

把一句话里的每个词当成会议室里的人：  
注意力 = 每个人按「议题相关性」给别人打分，再混合信息。  
不必会推公式，但要记住：**它解决的是序列建模的效率与长依赖。**

## 接到下一站

基座训完还「不太听话」——下一课 **模型微调**：如何把基座变成可用的对话/领域模型。
`;

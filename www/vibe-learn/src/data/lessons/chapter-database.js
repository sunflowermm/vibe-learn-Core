/** 第五章 · 数据库与缓存 */
export default `# 第五章 · 数据库与缓存

> 后端岗八股三大件之一（另两件：操作系统、计算机网络）。  
> 2026 春招面经高频：MySQL 索引与事务、Redis 数据结构与持久化、缓存三大问题、分布式事务与 MQ 入门。  
> 本框按「存什么 → 怎么查得快 → 怎么保证对 → 怎么扛热点」展开。

## 与前后章

| 章 | 关系 |
|----|------|
| **DSA** | B+ 树、跳表、哈希、LRU 的工程落地 |
| **网络** | 连接、超时、主从复制的网络假设 |
| **语言** | SQL / 驱动 / ORM 建立在某运行时上 |
| **XRK** | 业务数据落盘、会话与限流可用 Redis 等（按产品选型） |

\`\`\`mermaid
flowchart TB
  SQL[关系模型与 SQL] --> IDX[MySQL 索引]
  SQL --> TXN[事务与隔离]
  IDX --> REDIS[Redis]
  TXN --> REDIS
  REDIS --> CACHE[缓存穿透击穿雪崩]
  REDIS --> MQ[消息队列入门]
\`\`\`

## 建议阅读顺序

1. **关系模型与 SQL**  
2. **MySQL 索引** → **事务与 MVCC**  
3. **Redis** → **缓存问题**  
4. **消息队列入门**（Kafka 等高吞吐直觉）

## 学完应能说明

- 为什么用 B+ 树做聚簇索引；最左前缀  
- ACID、隔离级别、脏读/幻读与 MVCC 直觉  
- Redis 五类结构 + RDB/AOF；缓存雪崩怎么防  
- MQ 解决什么：削峰、解耦、最终一致性  
`;

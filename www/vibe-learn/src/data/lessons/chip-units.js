/** 处理单元与存储层次 */
export default `# 处理单元与存储

> 处理单元（\*U）负责计算；存储层次决定「数据离 CPU 有多远」。  
> 二者共同解释性能体感：算得快 ≠ 读得快。

## 本课分块

| 块 | 目标 |
|----|------|
| **处理单元分工** | CPU / GPU / 加速器各擅长什么 |
| **存储层次** | 寄存器 → 缓存 → 内存 → 磁盘 |
| **与 OS / I/O** | 调度、缺页、网卡作为设备 |
| **选型误区** | 后端入门真正依赖什么 |
| **出桥** | 语言章 · 网络章 |

---

## 1. 处理单元分工

| 单元 | 擅长 | 直觉 |
|------|------|------|
| **CPU** | 通用逻辑、分支、系统调用路径 | 全能调度与决策 |
| **GPU** | 大规模同质并行（图形、部分 AI） | 同一算式重复极多次 |
| **NPU / TPU 等** | 特定张量/推理负载 | 专用加速 |
| **内存/存储控制器** | 搬移数据 | 后勤通路 |

\`\`\`mermaid
flowchart LR
  P[进程] --> OS[OS 调度]
  OS --> CPU[CPU]
  P -. API .-> DRV[加速器驱动]
  DRV --> GPU[GPU 等]
  CPU <--> RAM[内存]
  GPU <--> VRAM[显存等]
\`\`\`

运行 XRK-AGT 主路径：主要消耗 **CPU + 内存 + 磁盘 + 网卡**。  
本地大模型 / 重图形推理才会强烈依赖 GPU。

---

## 2. 存储层次（关键）

越靠近 CPU：**越快、越小、越贵**；越靠外：**越慢、越大、越便宜**。

| 层次 | 典型角色 | 速度量级（直觉） |
|------|----------|------------------|
| **寄存器** | CPU 内临时槽位 | 最快 |
| **Cache（缓存）** | CPU 旁的高速缓冲 | 极快 |
| **内存（RAM）** | 运行中的主工作区 | 快；断电丢失 |
| **SSD / HDD** | 持久化文件与交换 | 慢于内存；持久 |

\`\`\`mermaid
flowchart TB
  REG[寄存器] --> L1[Cache]
  L1 --> RAM[内存 RAM]
  RAM --> DISK[磁盘 SSD/HDD]
\`\`\`

工程含义：

| 现象 | 可能原因 |
|------|----------|
| CPU 不高但很慢 | 频繁读盘、缺页、锁等待 |
| 内存涨满后变卡 | 换页到磁盘，延迟上升 |
| 重启后数据还在 | 已落在持久存储；仅在内存中的会丢 |

程序「打开的文件」「跑着的进程」工作集主要在 **内存**；真正长期保存靠 **磁盘上的文件系统**（见系统的本质 · 文件抽象）。

---

## 3. 与 OS、I/O 的关系

- OS 将进程调度到 **CPU**；加速器经驱动与专用 API 暴露  
- **缺页**：访问的虚拟地址不在物理内存时，内核从磁盘装入（伴随停顿）  
- **网卡** 属于 I/O 设备：收发包最终仍要进入内存，再交给协议栈与进程  

---

## 4. 选型误区

| 误区 | 更准确 |
|------|--------|
| 有 GPU 即可弱化 CPU | Web、API、数据库、OS 本身大量占用 CPU |
| 服务器必须有独显 | 多数 API 主机可无独显 |
| 芯片越新项目越能跑 | 缺运行时、依赖或权限时，换芯片无效 |

---

## 5. 下一步

- **第二章 · 计算机语言** — 源码如何变成 CPU 上的指令流  
- **第三章 · 计算机网络** — 多机经网卡与协议栈对话  
- 回看 **软硬件联动** — 中断与 I/O 如何喂饱本课的单元与存储  
`;

/** 番外 · 端口服务 — 谁连上代理引擎 */
export default `# 端口服务 · 系统代理 · Coding Agent

> 上一张把代理引擎拆成「入口 + 决策 + 出口」。  
> 这里只盯**入口**：引擎在本机 listen 的端口。换位想——系统代理开了、浏览器通了、Agent 却超时，多半是 Agent **根本没连上这个入口**。

## 1. 先记住：服务 = 地址 + 端口

Clash（以及同类工具）本质是本机上的一个**代理应用服务**，例如：

\`\`\`
http://127.0.0.1:7890
\`\`\`

- \`127.0.0.1\`：本机  
- \`7890\`：它监听的端口（**以 Verge / 手机端设置里显示的为准**，常见还有 7897 等）

配置文件里可能叫 \`mixed-port\`（HTTP+SOCKS 合在一个端口）、\`port\`、\`socks-port\`——对你来说，记界面上的那一个数字就够。

> **几乎所有要「走代理」的程序配置，填的都是这类服务的主机和端口**，不是填机场节点域名。

## 2. 「系统代理」只是帮一部分程序填好了

点开 **系统代理**，操作系统大致会记下：

\`\`\`
HTTP/HTTPS 代理 → 127.0.0.1:<你的端口>
\`\`\`

- **会自动去读的**：多数浏览器、不少桌面软件  
- **常常不读的**：终端、\`git\`、\`npm\`、\`curl\`、Docker、**Coding Agent / CLI**

所以：

| 现象 | 含义 |
|------|------|
| 浏览器能上、Agent 不行 | Agent 没连上本机代理端口 |
| 托盘绿了、终端仍直连 | 绿的是服务在跑；终端没被配置去用它 |

不是「只有 Clash 才这样」——任何本机代理服务都一样：**不填端口（或不走 TUN），顽固程序就不会来。**

## 3. 给 Agent / 终端「说端口」

把下面的 \`7890\` 换成你 Verge 里看到的端口。

**PowerShell（当前窗口）**

\`\`\`powershell
$env:HTTP_PROXY  = "http://127.0.0.1:7890"
$env:HTTPS_PROXY = "http://127.0.0.1:7890"
$env:ALL_PROXY   = "socks5://127.0.0.1:7890"
\`\`\`

**bash / zsh**

\`\`\`bash
export HTTP_PROXY=http://127.0.0.1:7890
export HTTPS_PROXY=http://127.0.0.1:7890
export ALL_PROXY=socks5://127.0.0.1:7890
\`\`\`

Git / npm 也可单独写：

\`\`\`bash
git config --global http.proxy  http://127.0.0.1:7890
npm config set proxy http://127.0.0.1:7890
\`\`\`

Agent / IDE：设置里找 Proxy，填 \`http://127.0.0.1:<端口>\`；若只认环境变量，**先在已 export 的终端里启动它**。

### 自检（避免瞎改）

1. Verge 已运行，节点能用，浏览器经系统代理正常。  
2. 设置里确认端口号，没有抄错别人的截图。  
3. **同一个**终端里 \`echo\` 一下 \`HTTPS_PROXY\`，有值再开 Agent。

## 4. TUN：另开一章说的「不想每个软件手填」

**TUN** 会建虚拟网卡，尽量把流量截进代理服务——适合游戏、死活不读代理设置的程序。  

它权限更高，也可能误伤局域网设备，所以：

- **日常最小路径**：订阅 → 选节点 → 系统代理（见下一张 Verge 步骤）  
- **Agent / 开发工具**：优先学会填本机端口或环境变量  
- **TUN**：等你熟悉上面两步，再单独打开、慢慢调  

口诀：**系统代理救浏览器；手填端口救 Agent；TUN 是进阶全局方案，不是入门必点。**
`;

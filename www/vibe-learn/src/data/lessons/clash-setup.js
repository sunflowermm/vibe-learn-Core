/** 番外 · Verge 最简上手 + 手机 + TUN 另述 */
export default `# 最简上手：订阅就够（Clash Verge）

> 换位思考：你刚拿到一串「订阅链接」，最怕打开客户端看到一堆 YAML、TUN、规则。  
> **入门其实可以极简：填订阅 → 选节点 → 开系统代理。** 先让浏览器通，再管 Agent 端口；TUN 放到后面。

电脑用 **Clash Verge**（或 Verge Rev）；手机用 **Clash for Android / Clash Meta for Android**。外壳不同，底下都是「本机开端口的代理服务」。

---

## 电脑 Verge：侧边栏五步（先背这个）

界面文案可能略有出入，路径直觉如下：

1. **侧边栏 → 订阅（Subscription / 配置）**  
   - 新建 / 导入  
   - 把服务商给你的 **订阅链接** 粘进去  
   - 点订阅 / 更新，等节点列表出来  

2. **选中这份订阅**（让它成为当前生效的配置）

3. **侧边栏 → 代理（Proxies）**  
   - 需要「先全部走代理、少折腾规则」时：点 **全局（Global）**  
   - 在列表里 **点选一个节点**（可先测延迟，选绿的）  

4. **侧边栏 → 首页（Home）**  
   - 打开 **系统代理（System Proxy）**  

5. 用浏览器试一个平时打不开的站——通了，说明：  
   **本机代理服务在听端口 + 系统已把浏览器指过去。**

> 日常更省流量的做法是用 **规则（Rule）** 模式让国内直连；刚上手、先验证「通路是否通」时，用 **全局 + 选中节点 + 系统代理** 最直观。

### 端口记在哪

首页或设置里找到 **Mixed Port / 端口**，记下数字。  
以后 Git、npm、Coding Agent 填的就是 \`127.0.0.1\` + 这个数——详见上一张「端口服务」。

### 一分钟自检

\`\`\`powershell
# 7890 换成你界面上的端口
curl.exe -I --proxy http://127.0.0.1:7890 https://www.cloudflare.com
\`\`\`

有 HTTP 头返回 ≈ 「本机代理服务 → 出网」正常。

---

## 手机：Clash for Android / Meta（对照记）

1. 配置里贴 **同一类订阅链接** → 更新  
2. 主界面点启动，首次允许 **VPN**（手机常用隧道承接流量）  
3. 选节点；需要先验证再考虑规则 / 全局  
4. 没点启动、没给 VPN 权限 ≈ 服务没真正接管，App 仍直连  

电脑和手机各跑自己的客户端更省心；不必强行「手机代理给电脑用」。

---

## TUN 与规则：另作理解（入门可跳过）

| 东西 | 什么时候才需要想 |
|------|------------------|
| **系统代理** | 入门必开：救浏览器 |
| **手填端口 / 环境变量** | 开发、Agent：程序不读系统代理时 |
| **规则（Rule）** | 通了之后：国内直连、按域名分流，少耗节点 |
| **TUN** | 仍有软件漏网、或想少配每个 App 时再开；权限高，先别当第一步 |

TUN 可以理解为：不只「通知软件来连端口」，而是尽量在系统网络层把流量导入这个代理服务。  
**先会订阅 + 系统代理，再学 TUN**，少踩「一点 TUN 全网怪问题」的坑。

---

## 订阅背后大概有什么（知道即可，不必手写）

订阅更新下来的配置，通常自带：本机端口、节点列表、DNS、规则。  
你用 Verge 侧边栏订阅，就是让客户端替你拉这份文件——**比手写 YAML 简单一个数量级**。

若哪天要读懂文件，抓四块就行：端口入口、节点/策略组、DNS、rules。  
正章对照：端口 ↔ TCP；规则 ↔ 路由；域名规则 ↔ DNS；本机服务 ↔ 正向代理。

## 安全习惯（短）

- 订阅链接别贴进公开仓库  
- 公共 Wi‑Fi 慎开「允许局域网连接」  
- 改了端口，记得同步改 Agent / 环境变量  
`;

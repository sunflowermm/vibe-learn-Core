/** 番外 · 代理引擎本质 — 与第一章计算机网络对照 */
export default `# 番外 · 代理引擎本质

> Clash（以及同类客户端）首先不是「加速器品牌」，而是一台跑在你电脑里的**代理引擎**：  
> 像迷你路由器一样做**选路**，像本地服务一样占用 **TCP 端口**，像正向代理一样替你的程序**代为出门**。  
> 第一章讲清的网络概念，在这里几乎都能一一对上号。

## 1. 引擎在网络里站哪一层位置？

第一章里：主机连上网 → 协议栈分层传 → IP 找主机 → 端口找进程 → 路由选下一跳 → DNS 把名字变 IP → 正/反代理决定「谁替谁说话」。

代理引擎插在**你的程序和真正出网路径之间**：

\`\`\`
浏览器 / Agent / git
        │  （应用层：我想访问某个 HTTPS 站点）
        ▼
【本机代理引擎】← 你电脑上的进程，监听某个端口
        │  查规则（像查路由表）
        ├─ DIRECT → 按正常网关出家用路由
        └─ PROXY  → 先送到某个「节点」（远端跳板）再去目标
        ▼
   运营商 / 互联网 / 目标服务器
\`\`\`

对照第一章：

| 正章概念 | 在代理引擎里长什么样 |
|----------|----------------------|
| **网络是什么** | 你的程序不再「直接」只认家用路由一条路，多了可选出口（节点） |
| **协议栈** | 引擎本身是应用层进程；它帮你处理的仍是下层 TCP/IP 流量 |
| **IP / 子网** | 规则常按 IP、GEOIP、网段决定直连或代理 |
| **TCP 端口** | 引擎在本机 **listen** 一个（或几个）端口，别的程序连过来才算「走代理」 |
| **路由与网关** | **规则表 ≈ 应用层路由表**：匹配域名/IP 后选下一跳（DIRECT / 某策略组） |
| **DNS** | 先解析或由引擎代理解析，再拿结果去匹配规则（名字对不对得上规则，很关键） |
| **正向代理** | 引擎更靠近**你**；程序知道（或被系统告知）「请连本机代理」 |
| **反向代理** | 第一章网关课：更靠近**服务器**的门面——和引擎不是同一侧，别混 |
| **边缘实务** | IP 池、Cloudflare、地域与路径过滤症状——解释「出口 / 入口」各站哪一侧 |

## 2. 「引擎」两个字：入口 + 决策 + 出口

拆成三块，就和第一章的「服务 / 路由 / 下一跳」同构：

### ① 入口：本机上的应用服务（↔ TCP 端口）

引擎启动后，本机出现类似：

\`\`\`
127.0.0.1:7890  ←  正在 listen 的本地服务（数字以客户端为准）
\`\`\`

这和「Web 听 443、数据库听 5432」是同一类事实：**端口标出本机哪个进程在提供服务**。  
Git、浏览器、Coding Agent 配置里填的「代理主机 + 端口」，填的就是这个入口——**不是填机场节点的公网 IP**。

> 不是只有 Clash 能当这个入口：任何在本机开 HTTP/SOCKS 代理端口的工具，网络角色都类似。品牌换了，**「连本机端口」这件事不变**。

### ② 决策：规则引擎（↔ 路由表）

第一章路由：看目标 IP → 查表 → 选下一跳。  
代理引擎：看域名 / IP / 进程等 → 查 **rules** → 选 DIRECT 或某个 **proxy-group**。

| 路由课 | 代理引擎 |
|--------|----------|
| 路由表条目 | \`DOMAIN-SUFFIX\`、\`IP-CIDR\`、\`GEOIP\`、\`MATCH\`… |
| 默认路由 | 常落到 \`MATCH\` 兜底（全局走代理或走某组） |
| 网关 | 策略组里当前选中的节点 / \`DIRECT\` |

三种常见模式，就是三种「默认路由策略」：

| 模式 | 直觉（用路由语言说） |
|------|----------------------|
| **rule** | 按表走：国内直连、名单走代理（日常推荐） |
| **global** | 几乎一律走代理下一跳（先验证通路时好用） |
| **direct** | 几乎一律直连（等于临时关掉代理意图） |

### ③ 出口：节点 / 订阅（↔ 可选的下一跳）

订阅拉下来的是一批**远端出口**（节点）+ 往往自带默认规则。  
对引擎来说：入口收齐你的流量，决策选路，出口才真正进广域网/互联网——呼应第一章 LAN → 网关 → WAN/Internet。

## 3. 正向代理引擎 vs 反向代理门面（务必分清）

第一章「反向代理与限流」讲的是服务器侧门面（Nginx 等）。  
代理引擎站在**客户端侧**：

| | 正向代理引擎（Clash 这类） | 反向代理（Nginx 等） |
|--|---------------------------|----------------------|
| 靠近谁 | 你的电脑 / 手机 | 网站机房 / CDN |
| 谁配置它 | 你（或系统代理替你指过去） | 运维 / 云厂商 |
| 你感知到谁 | 常感知「我开了代理」 | 常只感知「我在访问某域名」 |
| 第一章比喻 | 出门前先问本地「导游」走哪条路 | 进大厦先到前台，再被领到具体房间 |

同一词「代理」，**方向相反**；番外练的是正向这一侧。

## 4. 流量什么时候才会进引擎？

引擎在听端口 ≠ 全机流量自动灌进去（除非另开 TUN 等接管，见后文）。

| 做法 | 和第一章的对应 | 谁会进引擎 |
|------|----------------|------------|
| **系统代理** | 改「默认怎么找代理」的系统设置 | 听话的浏览器等 GUI |
| **手写端口 / 环境变量** | 进程级明确指定要连的 **IP:端口** | 终端、Agent、git… |
| **TUN**（进阶） | 更像在系统里多挂一块「虚拟网卡」截流 | 尽量更多进程 |

所以你会看到：浏览器通了、Agent 不通——不是引擎「坏了」，是 **Agent 这个进程根本没连到引擎的入口端口**（下一张专讲）。

## 5. 用一张「第一章复盘」收束

打开网页时，正章链路是：DNS → IP → 路由出网 → TCP → TLS/HTTP。  
开着代理引擎时，中间多了一跳**本地决策**：

1. 程序（或系统）把连接指向 **本机引擎端口**（传输层：找到引擎这个进程）  
2. 引擎用 **DNS/域名/IP 规则** 决定直连还是节点（网络层选路的应用版）  
3. 若走节点：你的包先到远端跳板，再去目标（多了一段 WAN 路径，延迟与带宽会变）  
4. 目标站仍按 HTTPS 等规矩工作——加密的是你与目标（或与节点）之间的通道，别和「引擎是否开启」混为一谈  

> **一句话**：代理引擎 = **本机端口上的正向代理服务** + **应用层路由（规则）** + **可选远端下一跳（节点）**。  
> Clash 只是常见实现之一；把第一章的端口、路由、DNS、正反向代理吃透，任何同类客户端都好懂。

下一张：入口端口如何配置进 Coding Agent；再下一张：Verge 用订阅把引擎「喂饱」的最简操作。
`;

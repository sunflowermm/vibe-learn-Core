/** HTTPS 与 DNS — 对齐课件 */
export default `# HTTPS 与 DNS

## HTTPS：加密的 HTTP

| | HTTP | HTTPS |
|--|------|-------|
| 传输 | 常为明文 | 经 TLS/SSL 加密 |
| 风险 | 易被窃听、篡改 | 机密性、完整性更好 |
| 地址 | \`http://\` | \`https://\`（常伴小锁） |

入门记法：**HTTPS ≈ HTTP + TLS（加密通道）**。

### TLS 握手在干什么（简化）

1. 客户端告知自己支持的加密能力  
2. 服务器选定算法，并发送**证书**  
3. 客户端验证证书（是不是可信机构签的、域名对不对等）  
4. 双方商定会话密钥  
5. 之后用密钥加密通信  

证书解决的是：「我是不是在和我想的那台服务器说话」，而不只是「通道有加密」。

---

## DNS：互联网的电话簿

> **DNS（Domain Name System）** 把好记的**域名**（\`www.example.com\`）翻译成机器用的**IP**（如 \`93.184.216.34\`）。采用层次化、分布式的数据库协作完成。

### 为什么需要 DNS？

人记名字容易，路由器认 IP。没有 DNS，你得手写一串数字才能上网。

### 解析大概怎么走

1. 你输入 \`www.example.com\`  
2. 先查浏览器 / 系统**本地缓存**  
3. 没有则问本地 DNS（常是运营商或你配的 8.8.8.8 等）  
4. 本地 DNS 再去问：根 → 顶级域（如 \`.com\`）→ 权威服务器…  
5. 得到 IP，返回给你，并常会缓存一段时间  

### 域名层次

| 层级 | 例子 | 谁在管（直觉） |
|------|------|----------------|
| 根 | \`.\` | 全球根服务器体系 |
| 顶级域 TLD | \`.com\` \`.cn\` \`.org\` | 相应管理机构 |
| 二级域 | \`example.com\` | 注册后由你/服务商管 |
| 子域 | \`www.\` \`mail.\` | 域名所有者配置 |

### 常见记录类型

| 类型 | 作用 |
|------|------|
| **A** | 域名 → IPv4 |
| **AAAA** | 域名 → IPv6 |
| **CNAME** | 别名 → 另一个域名 |
| **MX** | 邮件服务器指向 |

### DNS 与 HTTPS 怎么串起来

打开 \`https://example.com\` 时，常常是：

**DNS 得到 IP → TCP 连接 → TLS 握手 → 再发 HTTP 请求**。

任一步失败，表现不同：解析失败会「找不到服务器」；证书不对会浏览器警告；等。
`;


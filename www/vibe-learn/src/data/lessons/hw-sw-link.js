/** 软硬件联动 */
export default `# 软硬件联动

> 一次用户操作，从输入设备到晶体管，经过多层翻译。  
> 本课给出分层模型，并补全 **中断、I/O、驱动** 在链路中的位置。

## 本课分块

| 块 | 目标 |
|----|------|
| **故事线** | 从输入到响应的因果顺序 |
| **分层翻译** | 应用 → 运行时 → 内核 → 驱动 → 硬件 |
| **中断与 I/O** | 设备如何「打断」CPU、数据如何进出 |
| **故障对照** | 现象落在哪一层 |
| **出桥** | 处理单元 / 环境 / 语言 / 网络 |

---

## 1. 故事线

\`\`\`mermaid
sequenceDiagram
  participant F as 输入设备
  participant App as 应用
  participant OS as 操作系统
  participant Drv as 驱动
  participant HW as 硬件
  F->>OS: 硬件中断 / 输入事件
  OS->>App: 投递事件
  App->>OS: 系统调用（读文件 / 发网络）
  OS->>Drv: 访问设备
  Drv->>HW: 寄存器 / DMA
  HW-->>Drv: 完成中断
  Drv-->>OS: 结果
  OS-->>App: 返回
\`\`\`

软件描述意图；硬件产生物理变化；OS 与驱动负责调度与翻译。

---

## 2. 分层翻译

| 层 | 职责 | 常见形态 |
|----|------|----------|
| 应用 | 业务与界面 | 浏览器、编辑器、\`node\` |
| 运行时 / 库 | 高级 API → 系统调用 | Node、libc、GUI 工具包 |
| 内核 | 调度、权限、协议栈入口 | 端口占用、权限拒绝 |
| 驱动 | 设备专用协议 | 网卡、显卡、磁盘驱动 |
| 硬件 | 电平、扇区、射频 | CPU、内存、磁盘、网卡 |

---

## 3. 中断与 I/O

| 概念 | 说明 |
|------|------|
| **中断** | 设备或时钟通知 CPU：「有事需处理」，可打断当前指令流 |
| **轮询** | CPU 主动反复查询设备状态（简单但费算力） |
| **I/O** | 与外设交换数据：键盘、磁盘、网卡、显示 |
| **DMA** | 设备与内存直接搬运数据，减轻 CPU 搬运负担 |

对后端的直觉：

- 磁盘慢、网络慢 → 进程常在 **等待 I/O**，而非一直占满 CPU  
- 「机器很卡但 CPU 不高」→ 可能卡在磁盘或锁等待  
- 网卡收包 → 中断 / 软中断 → 协议栈 → 套接字 → 你的进程  

主板 **总线** 可理解为各部件之间的数据通路（CPU、内存、外设控制器经其互联）；入门只需建立「不是所有部件都直接互连」的印象。

---

## 4. 故障对照

| 现象 | 常在的层次 |
|------|------------|
| 风扇狂转、过热 | CPU/GPU 高负载或散热不足 |
| 点击无响应 | 应用主线程阻塞或进程卡死 |
| 磁盘已满 | 块设备容量 / 文件系统配额 |
| 插线却不上网 | 链路、驱动、协议栈或路由 |
| 进程闪退 | 未处理异常或非法内存访问 |

---

## 5. 出桥

- **处理单元与存储** — 谁在计算、数据落在哪一层存储  
- **环境与终端** — Shell 发起的是新的进程与系统调用  
- **计算机语言** — 编译/解释结果最终仍是 CPU 指令  
- **计算机网络** — 网卡是一类 I/O；套接字是其软件门面  
`;

/** IP、子网、MAC、ARP — 对齐课件 IP 相关页 */
export default `# IP 地址、子网与 MAC / ARP

> **IP 地址**在网络中标识一台设备，帮助路由找到目标。IPv4 是 32 位，常写成四个 0–255 的十进制数，用点分隔（点分十进制），如 \`192.168.1.100\`。

## 三个「号码」对照（先建立直觉）

| | IP 地址 | 域名 | MAC 地址 |
|--|---------|------|----------|
| 类比 | 门牌号（位置） | 好记的街道名 | 身份证（硬件） |
| 层次 | 网络层 | 应用侧命名 | 数据链路层 |
| 例子 | \`192.168.1.1\` | \`www.baidu.com\` | \`00:1A:2B:3C:4D:5E\` |
| 可变性 | 可配、DHCP 常会变 | DNS 可改指向 | 出厂绑定，通常不变 |
| 谁爱看 | 路由器 | 人 / DNS | 交换机 |

> 访问网站：输入域名 → DNS 变成 IP → 再按 IP 把包送过去。

## IP 做什么

- **标识设备**：在约定范围内区分主机  
- **路由寻址**：路由器靠它选下一跳  
- **划分网络**：配合掩码分「网络部分 / 主机部分」  
- **通信基础**：几乎所有跨主机通信都离不开地址

## IPv4 vs IPv6

| | IPv4 | IPv6 |
|--|------|------|
| 长度 | 32 位 | 128 位 |
| 样子 | \`192.168.1.1\` | \`2001:0db8:85a3::8a2e:0370:7334\` |
| 地址量 | 约 42 亿，公网紧张 | 极大，为长期扩展设计 |
| 现状 | 仍最常见 | 逐步部署中 |

## 地址分类与特殊地址（IPv4 入门）

传统分类（了解即可，现代更常用 CIDR）：

| 类 | 大致范围 | 默认掩码直觉 | 常见用途感 |
|----|----------|--------------|------------|
| A | 1.0.0.0–126… | \`255.0.0.0\` | 超大网 |
| B | 128–191… | \`255.255.0.0\` | 中大型 |
| C | 192–223… | \`255.255.255.0\` | 小型 / 家庭常见感 |

**特殊地址：**

- \`127.0.0.1\`：本机回环（自己访问自己）  
- \`10.x.x.x\`、\`172.16–31.x.x\`、\`192.168.x.x\`：**私有地址**（内网用，公网不直接路由）

## 子网掩码（Subnet Mask）

子网掩码也是 32 位，用来区分 IP 里哪一段是**网络**、哪一段是**主机**。常写成 \`255.255.255.0\`，或 CIDR \`/24\`（前 24 位是网络位）。

### 例子

- IP：\`192.168.1.100\`  
- 掩码：\`255.255.255.0\`（\`/24\`）  
- 网络 ID：\`192.168.1.0\`  
- 主机部分：\`100\`  
- 同网段可用主机大约：254 个（要去掉网络地址与广播地址）

### 为什么需要掩码？

- 把大网划成小网，好管理  
- 路由器判断「目标是否在同一网络」  
- 合理分配地址，减少浪费  
- 不同子网可配不同安全策略  

### 常用掩码速查

| 掩码 | CIDR | 主机数约 |
|------|------|----------|
| \`255.0.0.0\` | /8 | 极多 |
| \`255.255.0.0\` | /16 | 约 6 万 |
| \`255.255.255.0\` | /24 | 254 |
| \`255.255.255.128\` | /25 | 126 |
| \`255.255.255.252\` | /30 | 2（点对点常见） |

公式直觉：可用主机 ≈ \`2^(主机位数) - 2\`（减网络地址、广播地址）。

## MAC 地址

- **是什么**：网卡的物理地址，48 位（6 字节），链路层识别设备  
- **格式**：\`00:1A:2B:3C:4D:5E\`（十六进制）  
- **组成**：前 24 位常为厂商标识（OUI），后 24 位厂商分配  

同一局域网内转发帧，交换机主要看 MAC。

## ARP：IP 怎么变成 MAC？

**ARP（Address Resolution Protocol）**：在局域网里，把「我要找的 IP」解析成「对应网卡的 MAC」。

典型过程：

1. 主机 A 要发给 \`192.168.1.100\`  
2. 先查本地 ARP 缓存  
3. 没有 → 广播 ARP 请求：「谁是 .100？把 MAC 告诉我」  
4. 目标机回复自己的 MAC  
5. A 写入缓存，再封装帧发出  

> 核心理解：MAC = 硬件身份证；IP = 网络门牌；ARP = 把门牌问成身份证（在局域网内）。
`;


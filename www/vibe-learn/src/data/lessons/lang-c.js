/** C */
export default `# C 语言

> **C** 是系统级基石：语法贴近机器，控制内存与硬件接口。  
> 操作系统、嵌入式、大量运行时与数据库内核用 C（或 C 风格）写成。  
> 本仓**无独立 C 子服**；学它是为了读懂「近金属」层与其它语言的底层依赖。

## 本课分块

| 块 | 目标 |
|----|------|
| **定位** | 近金属 / 可移植汇编直觉 |
| **执行模型** | 编译链接、手动内存 |
| **与其它语言** | FFI、运行时用 C 实现 |
| **八股 × 业务** | 指针、栈堆、未定义行为 |
| **本仓** | 为何不单列 runtime |

---

## 1. 定位

| 特征 | 说明 |
|------|------|
| **编译到机器码** | 少运行时；性能与可控性强 |
| **手动内存** | \`malloc\` / \`free\`；责任在人 |
| **指针** | 直接操纵地址 |
| **短板** | 易出现缓冲区溢出、UAF；工程约束靠规范与工具 |

层次：系统 / 近金属。Rust/Go/Java/Node 的运行时底层大量「站在 C 的肩膀上」。

---

## 2. 执行模型

\`\`\`
.c ──编译──► .o 目标文件 ──链接──► 可执行文件 / 动态库
\`\`\`

| 词 | 直觉 |
|----|------|
| **头文件 / 源文件** | 声明与实现分离 |
| **栈 / 堆** | 自动局部 vs 动态分配 |
| **未定义行为 UB** | 规范不管的操作；优化器可假设不发生 |

---

## 3. 与其它语言的关系

| 语言 | 关系 |
|------|------|
| **Python / Node** | 扩展/原生模块常 C/C++ |
| **Rust** | FFI 调 C；\`unsafe\` 边界 |
| **Go** | cgo |
| **Java** | JNI |

理解 C，有助于理解「为何脚本慢、扩展快」「崩溃在原生库」等现象。

---

## 4. 与本仓

不单独提供 \`cserver\`：主服已是 Node；性能热点优先 **Rust/Go 子服**（更安全的工程默认）。  
需要极底层库时，通常经 Rust/Go/Python 扩展间接使用，而不是主仓直接维护 C 插件树。

## 八股 × 业务串联

| 常考词 | 一句话 | 业务里长什么样 |
|--------|--------|----------------|
| **指针 vs 数组** | 地址与连续内存 | 越界写入 = 安全事故 |
| **栈溢出 / 堆破坏** | 调用过深或写坏堆元数据 | 神秘崩溃、难复现 |
| **野指针 / UAF** | 指向无效或已释放 | 安全漏洞经典来源 |
| **静态 vs 动态链接** | 库打进二进制 vs 运行时加载 | 部署依赖是否齐 |
| **\`volatile\` / 内存序（名）** | 防优化掉的访问 | 驱动、并发底层 |

## 下一步

**Rust**（安全的系统级替代叙事）· **编译与运行时**（回看 AOT）· **语言版图**。
`;

/** C — 场景 + 特性加厚 */
export default `# C 语言（语言）

> **分类：编程语言。** 近金属、手动内存、无 GC、ABI/FFI 基石。  
> 本仓 **无独立 C 子服**；用来理解运行时、操作系统、Node/Python/Rust 扩展底层。

## 本课你要带走什么

1. **什么场景仍要写 C、什么场景该用 Rust/Go 等更高层语言**  
2. 指针、内存、UB、编译链接等特性怎么讲清  
3. C 在本仓生态里（FFI、扩展）的位置，与 Rust 子服如何对照

---

## 1. 使用场景：何时用、何时不用

### 1.1 适合用 C 的场景

| 场景 | 为什么选 C |
|------|------------|
| **操作系统 / 驱动 / 嵌入式** | 直接硬件、最小运行时 |
| **性能关键库的实现语言** | sqlite、redis、openssl、V8 底层 |
| **FFI 边界** | 几乎所有语言都能调 C ABI |
| **已有庞大 C 代码库** | 重写成本极高 |
| **理解指针与内存模型** | 学 Rust/Go/JS 扩展的「地基」 |
| **极资源受限环境** | 无 GC、可预测内存 |

### 1.2 不适合 / 通常别用的场景

| 场景 | 更好选择 | 原因 |
|------|----------|------|
| **本仓主服业务** | **JavaScript** | 无 C 运行时集成 |
| **新 Web API 子服** | **Go / Python / Node** | 开发效率与安全 |
| **要内存安全的新项目** | **Rust** | C 的 UB 与手动管理 |
| **高并发网络服务（新写）** | **Go** | 标准库 + GC 省心 |
| **快速业务 CRUD** | 任意高级语言 | C 样板与 bug 成本高 |
| **字符串与容器重度业务** | Python / Java | 标准库与 GC |

### 1.3 与 Rust / C++ / Go 怎么选（口述）

| 需求 | 更偏向 |
|------|--------|
| 极致可控 + 已有 C 库 | **C**（或 Rust FFI 调 C） |
| 安全 + 性能 + 现代工具 | **Rust** rustserver |
| 大游戏/历史 C++ 资产 | **C++** |
| 云原生 HTTP 子服 | **Go** |

\`\`\`mermaid
flowchart TB
  App[高级语言应用] --> FFI[C ABI / FFI]
  FFI --> CLib[C 库 / 扩展]
  CLib --> OS[操作系统 / 硬件]
\`\`\`

---

## 2. 语言特性（必须讲清楚）

### 2.1 内存模型

| 点 | 说明 |
|----|------|
| **栈 vs 堆** | 局部自动分配 vs \`malloc/free\` |
| **指针** | 存地址；可算术；越界 = UB |
| **数组与指针衰退** | 传参常变指针；丢失长度信息 |
| **\`const\`** | 只读约定；可被强转破坏 |
| **对齐与结构体填充** | 性能与 ABI |

### 2.2 类型与编译

| 点 | 说明 |
|----|------|
| **静态弱类型倾向** | 隐式转换多；要自觉 |
| **预处理** | \`#include\` \`#define\`；头文件卫士 |
| **编译链接** | .c → .o → 可执行/库；符号解析 |
| **UB（未定义行为）** | 越界、用后释放、有符号溢出… |

### 2.3 函数与模块

| 点 | 说明 |
|----|------|
| **声明 vs 定义** | 头文件声明、源文件定义 |
| **\`static\`** | 内部链接 / 静态存储期 |
| **\`extern\`** | 跨编译单元 |

### 2.4 与系统

| 点 | 说明 |
|----|------|
| **系统调用包装** | 标准库与 OS 之间 |
| **信号、文件描述符** | Unix 编程基础 |
| **FFI** | Node N-API、Python C 扩展、Rust \`extern "C"\` |

---

## 3. 工具链

| 工具 | 作用 |
|------|------|
| **gcc / clang** | 编译器 |
| **make / cmake / meson** | 构建系统 |
| **gdb / lldb** | 调试 |
| **valgrind / ASan** | 内存错误检测 |
| **pkg-config** | 库依赖发现 |

---

## 4. 与本仓

| 项 | 说明 |
|----|------|
| **无 C 子服** | 业务用 JS + 各语言子服 |
| **间接存在** | V8（Node）、Python C 扩展、Rust FFI 调 C 库 |
| **学习目的** | 读懂「为什么 Rust 要有所有权」 |
| **扩展边界** | 若写 Native 扩展，C ABI 是通用语言 |

| 对照 | 关系 |
|------|------|
| **Rust** | 想要 C 的控制力 + 编译期安全网 |
| **Go** | 有 GC、更易写网络服务 |
| **Node** | 主服用 JS；底层 libuv/V8 含 C/C++ |

---

## 5. 大厂真题

| 题 | 答法要点 |
|----|----------|
| 指针与数组 | 传参衰退、\`sizeof\` 数组 vs 指针 |
| 栈溢出 vs 堆损坏 | 症状不同 |
| 野指针 / 用后释放 | 定义与防范 |
| \`static\` 三含义 | 函数内、文件内、全局 |
| 内存对齐 | 结构体大小与 padding |
| 为什么还有 C | ABI、嵌入式、历史库 |

---

## 高星仓库（读源码 / 对照本仓）

> stars 量为公开量级参考（会变）；重点是**学什么**与**本仓落点**，不是追星。

| 仓库 | 量级参考 | 学什么 | 对本仓落点 |
|------|----------|--------|------------|
| [torvalds/linux](https://github.com/torvalds/linux) | ⭐ 二十万级 | 内核 C、系统调用、驱动边界 | **近金属对照**；理解 OS 与用户态服务 |
| [redis/redis](https://github.com/redis/redis) | ⭐ 六万级 | 事件循环、内存结构、网络服务器 C | 本仓 **Redis** 依赖的实现语言直觉 |
| [libuv/libuv](https://github.com/libuv/libuv) | ⭐ 两万级 | 跨平台异步 I/O（Node 底层） | **主服 Node** 事件循环的 C 基座 |

---

## 6. 下一步

**Rust** · **Go** · **操作系统本质** · **编译与运行时** · **语言栈** · **技术选型**。  
可选：扫 Redis 或 libuv 的 README，把「C 在系统层」和「业务在 core/」分开。
`;

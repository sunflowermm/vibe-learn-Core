/** Java */
export default `# Java

> **Java** 以「一次编写、到处运行」闻名：源码编译为 **字节码**，由 **JVM** 执行。  
> 企业中间件、Spring、JDBC 生态深厚。本仓子服 **\`jserver\`（:8003，Java 21 + Spring Boot）**。

## 本课分块

| 块 | 目标 |
|----|------|
| **定位** | 托管运行时与企业后端 |
| **执行模型** | javac → 字节码 → JVM / JIT |
| **工具链** | Maven / Gradle、JDK |
| **本仓** | jserver |
| **八股 × 业务** | JVM 内存、GC、集合、并发 |

---

## 1. 定位

| 特征 | 说明 |
|------|------|
| **静态类型、面向对象** | 类、接口、强工程约束 |
| **JVM** | 跨平台；GC 管理内存 |
| **生态** | Spring、各类中间件、大数据周边 |
| **短板** | 启动与内存占用相对重；语法偏冗长 |

适合：对接遗留 Java 库、事务型数据访问、已有 Spring 组件复用。

---

## 2. 执行模型

\`\`\`
.java ──javac──► .class 字节码 ──► JVM（解释 + JIT）──► 机器码热点
\`\`\`

| 词 | 直觉 |
|----|------|
| **JDK / JRE** | 开发工具包 / 运行环境 |
| **字节码** | 平台无关的中间表示 |
| **JIT** | 热点代码运行中编译加速 |
| **GC** | 自动回收；停顿与调优是运维话题 |

---

## 3. 工具链

| 项 | 说明 |
|----|------|
| **Maven / Gradle** | 依赖与构建 |
| **\`pom.xml\` / \`build.gradle\`** | 项目契约 |
| **Spring Boot** | 本仓 jserver 示例栈 |

---

## 4. 与本仓

| 项 | 落点 |
|----|------|
| Runtime | \`jserver\` |
| 端口 | 8003 |
| 典型 | 企业库、JDBC、Spring 能力复用 |

## 八股 × 业务串联

| 常考词 | 一句话 | 业务里长什么样 |
|--------|--------|----------------|
| **堆 / 栈 / 方法区（名）** | JVM 运行时数据区划分 | OOM 要分清是堆溢出还是栈过深 |
| **GC 种类（名）** | 年轻代/老年代回收策略 | 吞吐 vs 低延迟选型 |
| **\`==\` vs \`equals\`** | 引用 vs 语义相等 | 字符串、实体比较 |
| **集合框架** | List/Map/Set 族 | 并发包 \`ConcurrentHashMap\` 等 |
| **\`synchronized\` / Lock** | 互斥 | 线程安全与死锁排查 |

## 下一步

**C#**（另一托管运行时）· **Kotlin**（可选 JVM 方言，本仓未单列节点）· 第四章 **语言栈**。
`;

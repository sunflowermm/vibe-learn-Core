/** JavaScript */
export default `# JavaScript（JS）

> **JavaScript** 是一门多范式脚本语言：浏览器里驱动页面，Node.js 里驱动服务端。  
> 本仓 **主服** 即用 JS 跑在 Node ≥ 26 上。

## 本课分块

| 块 | 目标 |
|----|------|
| **定位** | 胶水语言与事件驱动 |
| **双宿主** | 浏览器 vs Node |
| **语法与模块** | 模块、异步、类型擦除邻居 |
| **工具链** | npm 生态与本仓 pnpm |
| **本仓** | 主服职责 |
| **八股 × 业务** | 事件循环、闭包、原型 |

---

## 1. 定位

| 特征 | 说明 |
|------|------|
| **动态类型** | 运行期决定类型；灵活也易踩坑 |
| **事件驱动 / 异步** | 回调、Promise、\`async/await\` 是日常 |
| **单线程 + 并发 I/O** | 主线程不堵死在等网/等盘（靠事件循环） |
| **生态** | npm 包数量极大；前后端都能写 |

层次上属「脚本与胶水」：适合把 Web、API、工具脚本粘在一起。

---

## 2. 双宿主：浏览器 vs Node

| | 浏览器 | Node.js（本仓主服） |
|--|--------|---------------------|
| 入口 | \`<script>\` / 打包后的前端 | \`node app\` / ESM \`import\` |
| 能力 | DOM、BOM、页面事件 | 文件、进程、TCP 监听、无 DOM |
| 安全 | 页面沙箱 | 本机用户权限 |
| 全局 | \`window\` | \`globalThis\`；本仓另有 Runtime 裸名 |

会写网页 JS ≠ 会运维本仓：缺的是 **Node API、模块系统、进程模型**。

---

## 3. 模块与异步（名称级）

| 概念 | 直觉 |
|------|------|
| **ESM** | \`import\` / \`export\`（本仓主路径） |
| **CommonJS** | 旧 \`require\`（遗留包仍见） |
| **Promise / async** | 异步流程的现代写法 |
| **闭包** | 函数记住定义时的环境 |
| **原型链** | 对象继承的底层机制（面试常考） |

TypeScript 是 JS 的超集，编译后仍是 JS——见 **TypeScript** 课。

---

## 4. 工具链

| 项 | 常见 | 本仓 |
|----|------|------|
| 运行时 | Node / Deno / Bun | **Node ≥ 26** |
| 包管理 | npm / yarn / pnpm | **仅 pnpm** |
| 锁文件 | package-lock / pnpm-lock | \`pnpm-lock.yaml\` |

安装 Node 通常自带 npm/npx；本仓刻意选用 pnpm（见包管理器课）。

---

## 5. 与本仓

主服：AgentRuntime、Loader、HTTP/www、AiWorkflow、LLM 编排——全在 Node/JS 进程。  
需要 Python/Go 等生态时，经 **子服** HTTP 调用，不在主服里「再嵌一套语言」。

## 八股 × 业务串联

| 常考词 | 一句话 | 业务里长什么样 |
|--------|--------|----------------|
| **事件循环** | 单线程如何处理大量 I/O | 高并发 API；CPU 重活应下放子服或 Worker |
| **微任务 / 宏任务** | Promise 与 \`setTimeout\` 排队差异 | 调试「先打哪行 log」 |
| **this / 箭头函数** | 绑定规则不同 | 回调里丢上下文 |
| **浅拷贝 / 深拷贝** | 引用共享问题 | 配置对象被意外改掉 |
| **内存泄漏直觉** | 闭包、全局缓存、未清理监听 | 长跑主服内存只涨不降 |

## 下一步

**TypeScript** · **接到本仓运行时** · 其它语言分课 · 第四章 **语言栈**。
`;

/** 语言版图 */
export default `# 语言版图

> 不背排行榜，只认 **层次**：离硬件近还是离业务近。  
> 看懂层次，才知道文档里的 C / Java / Python / JS 各自站在哪一层。

## 和上一课的关系

上一课讲 **翻译发生在何时**（编译 vs 运行时）。  
本课把常见语言放进同一张地图，避免「每出一个名字就慌」。

## 三层直觉

| 层次 | 例子 | 常见用途 | 交付直觉 |
|------|------|----------|----------|
| **系统 / 近金属** | C、Rust、Go | OS 组件、高并发服务、工具链 | 常编译成可执行文件 |
| **托管 / 企业运行时** | Java、C# | 大型后端 | 常先到字节码，再由 **语言虚拟机** 执行 |
| **脚本 / 胶水 / 全栈** | JavaScript、Python、TypeScript | Web、自动化、AI 脚本、Node 后端 | 常依赖 **运行时** 已安装 |

\`\`\`mermaid
flowchart TB
  H[硬件 / OS] --> S[系统级语言]
  S --> M[托管运行时语言]
  M --> G[脚本与胶水语言]
  G --> APP[业务应用]
\`\`\`

### 本课必分清的几个名字

| 词 | 人话 |
|----|------|
| **JavaScript（JS）** | 网页脚本起家；在 Node 上也能做后端 |
| **TypeScript（TS）** | JS + 类型；**编译/擦除后仍是 JS** 再跑 |
| **Python** | 另一支常见脚本/数据语言；**运行时是 CPython 等**，与 Node 不是同一个 |
| **Java** | 常编译到字节码，由 **JVM** 执行 |

## 同构的幻觉（重要）

「前端也是 JS，后端也是 JS」——语法亲缘近，**运行时不同**：

| | 浏览器 | Node.js |
|--|--------|---------|
| 能做什么 | DOM、页面事件 | 文件、进程、监听端口 |
| 安全模型 | 网页沙箱 | 本机权限（更大） |

所以：会写网页 JS ≠ 自动会运维 Node 服务；第一章的 PATH / 端口问题仍然成立。

## 下一步

**接到本仓运行时** — 把版图收束到 XRK-AGT 的具体契约。
`;

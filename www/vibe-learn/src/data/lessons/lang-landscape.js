/** 语言版图 */
export default `# 语言版图

> 按**层次**理解语言：靠近硬件还是靠近业务应用。  
> 本仓坐标：主服使用 **Node.js（JavaScript）**；子服覆盖 **Python、Go、PHP、Java、.NET、Rust**（详见第四章「语言栈」）。

## 与上一课的关系

上一课说明翻译发生在编译期还是运行期。  
本课将常见语言放入同一张层次图，建立选型直觉。

## 三层结构

| 层次 | 示例 | 常见用途 | 交付形态 |
|------|------|----------|----------|
| **系统 / 近金属** | C、Rust、Go | 系统组件、高并发服务、工具链 | 多编译为可执行文件 |
| **托管运行时** | Java、C# | 大型后端与企业中间件 | 字节码 + 语言虚拟机（JVM / CLR） |
| **脚本与胶水** | JavaScript、Python、TypeScript、PHP | Web、自动化、数据分析、应用胶水 | 依赖已安装的运行时 |

\`\`\`mermaid
flowchart TB
  H["硬件 / OS"] --> S[系统级语言]
  S --> M[托管运行时语言]
  M --> G[脚本与胶水语言]
  G --> APP[业务应用]
\`\`\`

### 关键名称

| 名称 | 说明 |
|------|------|
| **JavaScript（JS）** | 浏览器与 Node 均可执行；本仓**主服**语言 |
| **TypeScript（TS）** | 带类型的 JS 超集；编译/擦除后仍以 JS 运行 |
| **Python** | 数据分析与 AI 生态成熟；本仓默认子服 \`pyserver\` |
| **Go / Rust** | 并发与性能路径强；对应 \`goserver\` / \`rustserver\` |
| **Java / C# / PHP** | 企业中间件、Windows/.NET、轻量 Web 脚本；各有对应子服 |

## 运行时边界

同一语言族在不同宿主中的能力不同：

| | 浏览器 | Node.js（主服） |
|--|--------|-----------------|
| 能力域 | DOM、页面事件 | 文件系统、进程、网络监听 |
| 安全模型 | 页面沙箱 | 本机权限模型 |

主服与子服分属不同进程与语言运行时：主服负责编排与 LLM；子服按语言优势承担专用能力。完整对照与选型见第四章 **语言栈**。

## 下一步

**接到本仓运行时** — 主服契约（Node + pnpm）；  
第四章 **语言栈** — 六套 runtime 与各语言优势。
`;

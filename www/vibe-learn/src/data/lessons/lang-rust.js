/** Rust — 场景 + 特性加厚 */
export default `# Rust（语言）

> **分类：编程语言。** 系统级性能、无 GC、用**所有权/借用**在编译期兜内存安全。  
> 本仓子服：**\`rustserver\`**。学习目标：知道「什么活该扔给 Rust」，而不是处处重写。

## 本课你要带走什么

1. **什么场景必须/适合用 Rust**，什么场景用 Go/Python 更划算  
2. 所有权、借用、\`Option\`/\`Result\`、Trait 等特性如何一句话说清  
3. 在 XRK 里它落在哪（子服），和主服 Node 如何分工

---

## 1. 使用场景：何时用、何时不用

### 1.1 适合用 Rust 的场景

| 场景 | 为什么选 Rust |
|------|----------------|
| **CPU 密集热点** | 解析、压缩、加解密、编解码、规则引擎；要榨干单机算力 |
| **内存安全要求高** | 想要 C/C++ 级控制，又怕悬垂指针、数据竞争 |
| **长期跑的基础组件** | 代理、过滤器、CLI 工具、边车；崩溃成本高 |
| **WASM / 嵌入与边界** | 浏览器 WASM、部分边缘场景；或给其它语言提供安全 FFI |
| **并发且怕数据竞争** | \`Send\`/\`Sync\` 在类型系统里约束跨线程 |

### 1.2 不适合 / 通常别用的场景

| 场景 | 更好选择 | 原因 |
|------|----------|------|
| **快速 CRUD、业务拼装** | Python / Java / Node | 开发速度、招聘、生态库 |
| **数据科学 / 训练脚本** | Python | 生态碾压 |
| **只要「丢个二进制高并发 HTTP」且团队不会 Rust** | Go | 更短学习曲线、GC 够用 |
| **主服插件业务逻辑** | JavaScript（本仓约定） | 主服是 Node；Rust 走子服 |
| **一周交付的活动页后端** | 任意 Web 框架语言 | Rust 编译期与学习成本不划算 |

### 1.3 和 C / Go / C++ 怎么选（口述）

| 需求 | 更偏向 |
|------|--------|
| 极致可控 + 已有 C 库 | C（或 Rust FFI 调 C） |
| 安全 + 性能 + 现代工具链 | **Rust** |
| 高并发网络服务 + 团队快速上手 | **Go** |
| 游戏/已有庞大 C++ 代码库 | C++（历史惯性） |

\`\`\`mermaid
flowchart TB
  Job[有一块很重或很危险的活] --> Q1{要无 GC 性能或强内存安全?}
  Q1 -->|否| GoPy[Go / Python / Java…]
  Q1 -->|是| Q2{团队能扛学习曲线?}
  Q2 -->|否| Go[先 Go 或把范围缩小]
  Q2 -->|是| Rust[Rust / rustserver]
\`\`\`

---

## 2. 语言特性（必须讲清楚）

### 2.1 所有权（Ownership）—— Rust 的第一公民规则

1. 每个值有且仅有一个**所有者**  
2. 所有者离开作用域 → 值被 **drop**（释放）  
3. 所有权可**移动（move）**；移动后原变量不能再用（除非类型实现 \`Copy\`）

这解决了：谁释放？释放两次？用后释放？——在**编译期**拦，而不是线上 core dump。

### 2.2 借用与生命周期

| 概念 | 规则 | 直觉 |
|------|------|------|
| \`&T\` | 可同时存在多个只读借用 | 多读者 |
| \`&mut T\` | 同时只能有一个可变借用 | 单写者 |
| 互斥 | 不能一边 \`&\` 一边 \`&mut\` | 禁止别名可变 |
| 生命周期 \`'a\` | 引用不能活过数据本身 | 杜绝悬垂引用 |

编译器推断大部分生命周期；函数返回引用时签名里常要写明。

### 2.3 \`Option\` / \`Result\`（没有「偷偷 null」）

| 类型 | 含义 |
|------|------|
| \`Option<T>\` | 有值 \`Some(t)\` 或没有 \`None\` |
| \`Result<T, E>\` | 成功 \`Ok(t)\` 或失败 \`Err(e)\` |
| \`?\` 运算符 | 错误向上传播，少写层层 match |

面试金句：**把空值与错误变成类型的一部分，强迫调用方处理。**

### 2.4 代数数据类型与模式匹配

- \`struct\` / \`enum\`（枚举可带数据）  
- \`match\` 必须穷尽；编译器检查漏分支  
- 适合状态机、协议解析、配置变体

### 2.5 Trait 与泛型

| 点 | 说明 |
|----|------|
| Trait | 行为抽象（像接口）；可默认实现 |
| 泛型 + bound | \`T: Read + Write\` |
| 零成本抽象目标 | 单态化后常接近手写性能 |
| orphan rule | 不能随意给外部类型实现外部 Trait（纪律） |

### 2.6 并发安全

| 点 | 说明 |
|----|------|
| \`Send\` | 所有权可以转到另一线程 |
| \`Sync\` | 可以安全地共享引用给多线程 |
| 数据竞争 | 安全 Rust 下编译器拒绝；\`unsafe\` 才自己负责 |
| async | 语言支持 \`async/await\`；**运行时**选 Tokio 等（语言不绑死一个 RT） |

### 2.7 \`unsafe\` 与 FFI

\`unsafe\` 块允许：裸指针、调用 unsafe 函数、可变静态等。  
正确用法：把不安全关在小模块里，对外暴露安全 API；或通过 FFI 调 C 库。

### 2.8 工具链

| 工具 | 作用 |
|------|------|
| **Cargo** | 构建、依赖、测试、发布 |
| **rustc** | 编译器 |
| **clippy / rustfmt** | 质量与格式 |
| **crate** | 包单元 |

---

## 3. 与本仓

| 项 | 说明 |
|----|------|
| Runtime | \`rustserver\` |
| 调用方式 | 主服 \`callSubserver\` → HTTP；**不**在主服进程内嵌 Rust |
| 典型插件 | 重计算、解析、安全相关短板能力 |
| 主服语言 | 仍是 JavaScript；Rust 是「加装引擎」 |

选型复述见第四章 **语言栈**、**技术选型**。

---

## 4. 大厂真题

| 题 | 答法要点 |
|----|----------|
| 所有权三条 | 口述 + move 例子 |
| 为何不能两个 \`&mut\` | 避免别名可变 → 数据竞争 |
| \`Option\` vs null | 类型强制处理 |
| 和 Go 怎么选 | GC vs 无 GC；学习曲线；场景表 |
| unsafe 何时用 | FFI、性能极限、已证明安全的封装 |

---

## 高星仓库（读源码 / 对照本仓）

> stars 量为公开量级参考（会变）；重点是**学什么**与**本仓落点**，不是追星。

| 仓库 | 量级参考 | 学什么 | 对本仓落点 |
|------|----------|--------|------------|
| [rust-lang/rust](https://github.com/rust-lang/rust) | ⭐ 十万级 | 所有权、借用、编译器诊断 | 理解 **rustserver**「加装引擎」的成本与收益 |
| [tokio-rs/tokio](https://github.com/tokio-rs/tokio) | ⭐ 两万级 | async runtime、任务与 I/O | 子服异步服务骨架 |
| [tokio-rs/axum](https://github.com/tokio-rs/axum) | ⭐ 两万级 | 类型安全路由、Extractor | HTTP 插件风格；主服仍用 \`callSubserver\` |

---

## 5. 下一步

**C**（近金属对照）· **Go**（并发服务对照）· **语言栈** · **子服务端** · **技术选型**。  
可选：读 Tokio/Axum 的「Hello HTTP」示例，对照 rustserver 进程边界。
`;

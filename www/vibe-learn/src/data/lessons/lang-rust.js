/** Rust */
export default `# Rust

> **Rust** 追求 **无 GC 的可控性能** 与 **内存安全**（所有权 / 借用检查在编译期完成）。  
> 本仓子服 **\`rustserver\`（:8005，Axum）**。

## 本课分块

| 块 | 目标 |
|----|------|
| **定位** | 系统级安全与热点路径 |
| **执行模型** | 编译、所有权、零成本抽象 |
| **工具链** | Cargo、crate |
| **本仓** | rustserver |
| **八股 × 业务** | 所有权、生命周期、Send/Sync |

---

## 1. 定位

| 特征 | 说明 |
|------|------|
| **编译型** | 生成高效本地代码 |
| **所有权** | 编译器阻止大量悬垂指针与数据竞争 |
| **FFI** | 与 C 等原生库互操作友好 |
| **短板** | 学习曲线陡；编译时间可较长 |

适合：CPU 热点、复杂正则、安全敏感组件、要「又快又不易用出 UAF」的路径。

---

## 2. 执行模型（名称级）

| 词 | 直觉 |
|----|------|
| **所有权 Ownership** | 每个值通常有一个主人 |
| **借用 Borrow** | 临时只读/可变引用，规则由编译器查 |
| **生命周期** | 引用能活多久（标注或推断） |
| **无 GC** | 确定时机释放；无 GC 停顿 |

「能通过编译」在 Rust 里往往意味着过了一大关安全审查。

---

## 3. 工具链

| 项 | 说明 |
|----|------|
| **Cargo** | 构建、测试、依赖（\`Cargo.toml\`） |
| **crates.io** | 包注册表 |
| **\`rustup\`** | 工具链版本管理 |

---

## 4. 与本仓

| 项 | 落点 |
|----|------|
| Runtime | \`rustserver\` |
| 端口 | 8005 |
| 框架示例 | Axum |
| 典型 | 性能热点、正则/FFI、安全向插件 |

## 八股 × 业务串联

| 常考词 | 一句话 | 业务里长什么样 |
|--------|--------|----------------|
| **所有权移动** | 赋值后原变量失效 | 编译错误逼你想清楚谁持有数据 |
| **\`&T\` / \`&mut T\`** | 共享借用 vs 独占可变 | 同时两个可变借用直接编译失败 |
| **\`Send\` / \`Sync\`** | 跨线程传递/共享的安全标记 | 并发 API 设计边界 |
| **\`Result\` / \`Option\`** | 错误与空值显式化 | 少空指针惊喜 |
| **unsafe** | 逃逸安全检查的明确标记 | 仅 FFI/底层需要时用，且要隔离 |

## 下一步

**C**（近金属对照）· **Go**（有 GC 的并发对照）· 第四章 **语言栈**。
`;

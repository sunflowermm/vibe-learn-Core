/** TypeScript */
export default `# TypeScript（TS）

> **TypeScript** = **带静态类型的 JavaScript 超集**。  
> 源码多为 \`.ts\` / \`.tsx\`；编译（或擦除类型）后，运行时仍是 **JavaScript**。

## 本课分块

| 块 | 目标 |
|----|------|
| **与 JS 关系** | 超集、擦除、兼容 |
| **类型在解决什么** | 接口契约、重构安全感 |
| **工具链** | \`tsc\`、\`tsconfig\`、与打包器 |
| **本仓** | 何时用 TS |
| **八股 × 业务** | 结构类型、泛型、any |

---

## 1. 与 JavaScript 的关系

\`\`\`
TypeScript 源码 ──编译/转译──► JavaScript ──► Node / 浏览器引擎执行
\`\`\`

| 点 | 说明 |
|----|------|
| **超集** | 合法 JS 多半是合法 TS（再逐步加类型） |
| **类型擦除** | 类型信息一般不进入运行时 |
| **不替代运行时** | 仍要装 Node；TS 不自己当 VM |

---

## 2. 类型解决什么业务问题

| 收益 | 例子 |
|------|------|
| **契约** | 函数参数、API 响应形状写清楚 |
| **重构** | 改字段名时编译器帮忙找引用 |
| **文档** | 类型即最准的局部文档 |
| **成本** | 学习曲线、\`any\` 滥用、构建一步 |

适合：多人协作、中大型前端/Node 项目、要与 OpenAPI 对齐的接口层。

---

## 3. 工具链直觉

| 词 | 作用 |
|----|------|
| **\`tsc\`** | 官方编译器 |
| **\`tsconfig.json\`** | 严格度、模块、目标 ES 版本 |
| **打包器** | Vite / esbuild / swc 等常边转译边打包 |
| **声明文件 \`.d.ts\`** | 给纯 JS 库补类型 |

---

## 4. 与本仓

本仓主服大量为 **原生 JS**（Node ESM）。  
业务 Core / 前端可用 TS 再编译；**子服**侧各语言自有类型系统（Java/C#/Go/Rust），不必强行全员 TS。

## 八股 × 业务串联

| 常考词 | 一句话 | 业务里长什么样 |
|--------|--------|----------------|
| **结构类型（鸭子类型）** | 形状对即可赋值 | 两个 interface 字段够用就能互通 |
| **\`any\` / \`unknown\`** | 放弃检查 vs 先收窄再用不 |
| **泛型** | 类型参数化 | \`Promise<T>\`、仓库通用 CRUD |
| **联合 / 交叉类型** | \`A \\| B\` / \`A & B\` | 状态机、配置合并 |
| **类型守卫** | 运行时收窄类型 | \`if ('id' in x)\` 后安全访问 |

## 下一步

**JavaScript**（运行时宿主）· **接到本仓** · 其它语言分课。
`;

/** Linux 基础指令 */
export default "# Linux 基础指令\n\n> 不求背诵手册，先建立 **空间感**：我在哪、有什么、怎么看、怎么动。  \n> 下列在 bash/zsh 下通用；PowerShell 有对应物但语法不同。\n\n## 知识串\n\n| 已学 | 本课动手 |\n|------|----------|\n| **终端 / Shell** | 在窗口里敲这些命令 |\n| **文件系统** | `cd` / `ls` 在目录树里走动 |\n| **进程** | `ps` / `kill` 看见并结束进程 |\n| **权限 / sudo** | 为何有时 Permission denied |\n\n```mermaid\nflowchart LR\n  A[\"我在哪 pwd\"] --> B[有什么 ls]\n  B --> C[去哪里 cd]\n  C --> D[\"看内容 / 改文件\"]\n  D --> E[\"看进程 / 权限\"]\n```\n\n## 本课分块\n\n| 块 | 内容 |\n|----|------|\n| **导航与感知** | pwd / ls / cd |\n| **读写与查找** | cat / mkdir / grep / find |\n| **进程与权限** | ps / kill / chmod / sudo |\n| **接到本仓** | 进入工作区并验证工具链 |\n\n---\n\n## 1. 导航与感知\n\n| 命令 | 作用 | 零基础备注 |\n|------|------|------------|\n| `pwd` | 打印当前目录 | 「我站在哪」 |\n| `ls` / `ls -la` | 列出文件 | `-la` 含隐藏文件（点开头） |\n| `cd 路径` | 进入目录 | `cd ..` 回上层；`cd ~` 回家目录 |\n| `tree`（若已装） | 树状查看 | 没有就先用 `ls` |\n\n路径两种直觉：\n\n- **绝对路径**：从根 `/`（或盘符）写起  \n- **相对路径**：从「你现在站的地方」写起  \n\n## 2. 读写与查找\n\n| 命令 | 作用 |\n|------|------|\n| `cat` / `less` | 看文件内容（`less` 可翻页） |\n| `mkdir` / `rm` / `cp` / `mv` | 建目录、删、拷、移 |\n| `grep` | 在文本里搜关键字 |\n| `find` | 按名/条件找文件 |\n\n<details>\n<summary>展开：危险操作的最小礼貌</summary>\n\n- 先 `ls` 再 `rm`；不确定就先 `mv` 到临时目录  \n- `rm -rf /` 类命令不要「试手气」  \n- 生产服务器上的 `chmod 777` 几乎总是错误捷径  \n</details>\n\n## 3. 进程与权限（常踩坑）\n\n| 命令 | 作用 |\n|------|------|\n| `ps` / `top` / `htop` | 看进程 |\n| `kill` / `kill -9` | 结束进程（先礼后兵；`-9` 强杀） |\n| `chmod` / `chown` | 改权限 / 所有者 |\n| `sudo` | 临时提升权限执行 |\n\n这些命令直接碰到 **系统的本质** 里讲的权限与进程抽象。  \n看见 `Permission denied`：先想「我是谁、文件属谁」，再决定要不要 `sudo`。\n\n## 4. 和本仓库的关系\n\n```bash\ncd XRK-AGT          # 进入工作区\nls                  # 应能看到 package.json\nnode -v && pnpm -v  # 验证工具链\n```\n\n指令是「手」；**第一章其它卡片**提供「装哪只手、站在哪片地」。\n\n## Windows 用户怎么办？\n\n| 你的环境 | 建议 |\n|----------|------|\n| PowerShell | 部分命令不同（`ls` 可用别名；`pwd` 也有） |\n| WSL / Git Bash | 上面表格基本可直接练 |\n| 纯 cmd | 建议换 Windows Terminal + PowerShell 或 WSL |\n\n## 下一步\n\n**Git 与工作区** — 用 `cd` / `ls` 确认你站在仓库根，再谈 clone 与托管平台。\n";

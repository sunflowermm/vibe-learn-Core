/** Linux 基础指令 */
export default `# Linux 基础指令

> 不求背诵手册，先建立 **空间感**：我在哪、有什么、怎么看、怎么动。  
> 下列在 bash/zsh 下通用；PowerShell 有对应物但语法不同。

## 本课分块

| 块 | 内容 |
|----|------|
| **导航与感知** | pwd / ls / cd |
| **读写与查找** | cat / mkdir / grep / find |
| **进程与权限** | ps / kill / chmod / sudo |
| **接到本仓** | 进入工作区并验证工具链 |

\`\`\`mermaid
flowchart LR
  A[我在哪 pwd] --> B[有什么 ls]
  B --> C[去哪里 cd]
  C --> D[看内容 / 改文件]
  D --> E[看进程 / 权限]
\`\`\`

---

## 1. 导航与感知

| 命令 | 作用 |
|------|------|
| \`pwd\` | 打印当前目录 |
| \`ls\` / \`ls -la\` | 列出文件（\`-la\` 含隐藏与详情） |
| \`cd 路径\` | 进入目录；\`cd ..\` 返回上层 |
| \`tree\`（若已装） | 树状查看结构 |

## 2. 读写与查找

| 命令 | 作用 |
|------|------|
| \`cat\` / \`less\` | 看文件内容 |
| \`mkdir\` / \`rm\` / \`cp\` / \`mv\` | 建目录、删、拷、移（\`rm\` 需谨慎） |
| \`grep\` | 在文本里搜关键字 |
| \`find\` | 按名/条件找文件 |

<details>
<summary>展开：危险操作的最小礼貌</summary>

- 先 \`ls\` 再 \`rm\`；不确定就先 \`mv\` 到临时目录  
- \`rm -rf /\` 类命令不要「试手气」  
- 生产服务器上的 \`chmod 777\` 几乎总是错误捷径
</details>

## 3. 进程与权限（常踩坑）

| 命令 | 作用 |
|------|------|
| \`ps\` / \`top\` / \`htop\` | 看进程 |
| \`kill\` / \`kill -9\` | 结束进程（先礼后兵） |
| \`chmod\` / \`chown\` | 改权限 / 所有者 |
| \`sudo\` | 临时提升权限执行 |

这些命令直接碰到 **系统的本质** 里讲的权限与进程抽象。

## 4. 和本仓库的关系

\`\`\`bash
cd XRK-AGT          # 进入工作区
ls                  # 应能看到 package.json
node -v && pnpm -v  # 验证工具链
\`\`\`

指令是「手」；**第一章其它卡片**提供「装哪只手、站在哪片地」。

## 下一步

**Git 与工作区** — 用 \`cd\` / \`ls\` 确认你站在仓库根，再谈 clone 与托管平台。
`;

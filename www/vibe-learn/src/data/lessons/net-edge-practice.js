/** IP 池、Cloudflare、地域与路径过滤、代理实务 */
export default `# 边缘实务：IP 池 · Cloudflare · 地域与代理

> 前几课讲清协议与门面；本课落到**上线常见的工程现实**：公网地址从哪来、边缘厂商做什么、机房地域怎么影响可达性、正向代理站在哪一侧。  
> 只讲**可观察现象与架构取舍**；跨境访问须遵守当地法规与服务条款，本课不提供绕过管制的操作指南。

## 本课分块

| 块 | 目标 |
|----|------|
| **公网 IP 与 IP 池** | 单 IP、弹性 IP、池化出口 |
| **Cloudflare 一类边缘** | DNS / 代理橙云 / CDN / WAF / Anycast |
| **地域与机房** | 为何常谈「香港节点」 |
| **路径过滤（墙）** | 技术症状，而非「怎么翻」 |
| **正向代理与跳板** | 与反代、CDN 的位置对照 |
| **排查清单** | 域名、证书、源站、出口 |
| **八股 × 业务** | 命中率、回源、出口白名单 |

---

## 1. 公网 IP 与 IP 池

部署到云主机时，你面对的不只是「有没有 IP」，而是**地址如何分配与复用**：

| 形态 | 直觉 |
|------|------|
| **固定公网 IP** | 一台机长期绑一个地址；DNS A 记录可直指它 |
| **弹性 / 浮动 IP** | 可在同区域内机器间迁移，方便换机不改对外地址 |
| **IP 池（地址池）** | 一组可分配地址：NAT 出站、负载均衡、代理出口、邮件发送等常从池中取用 |
| **共享出口** | 多台内网机经同一公网地址 PAT 出网（家用路由即此） |

与 **路由 / NAT** 课的衔接：动态 NAT「从池里借公网地址」；云厂商的「出口 IP 池」是同一直觉的产品化。

\`\`\`
用户 ──► 边缘/LB（可能多 IP / Anycast）
              │
              ▼
         源站（某区域一台或多台机，各有内网/弹性 IP）
\`\`\`

记忆：**DNS 指到的是「对外可见的入口地址」**；源站真实 IP 可以藏在反代 / CDN 后面。

---

## 2. Cloudflare（及同类边缘）在做什么

Cloudflare 把多件事叠在「域名 ↔ 互联网」之间，不必一次全开：

| 能力 | 你在控制台里常看到的 | 网络课对照 |
|------|----------------------|------------|
| **权威 DNS** | 把 NS 指到 CF；管 A/AAAA/CNAME… | DNS 课：权威在谁手里 |
| **代理（橙云）** | 访客先打到 CF 边缘，再回源 | 反向代理 + Anycast 入口 |
| **CDN** | 静态缓存、就近命中 | 反代课 CDN 块 |
| **TLS** | 边缘证书；回源可再加密 | HTTPS / TLS 终止 |
| **WAF / 限流** | 拦扫描、限制速率 | 反代课限流与防护 |
| **Workers 等** | 边缘可编程 | 应用层逻辑前移 |

**橙云 vs 灰云（简化）**：

- **代理开启**：对外解析到 CF 任播地址；源站 IP 不易直接暴露  
- **仅 DNS**：解析直接给出你的源站 IP；无边缘缓冲与 WAF  

**Anycast**：同一服务地址在多地通告，流量进「最近/优选」的 PoP——解释「为什么全球用户都像连到同一个 IP」。

与自建 Nginx 反代的关系：自建管「你的机房门口」；CF 管「全球门口 + DNS + 可选缓存」。可叠加：CF → 你的反代 → 应用。

---

## 3. 地域：为什么常提到香港服务器

延迟与管辖都和**物理/网络距离、跨境链路**有关：

| 因素 | 影响 |
|------|------|
| **RTT** | 用户 ↔ 机房跳数与海底光缆路径；东亚用户访问港/日/新常比欧美近 |
| **跨境质量** | 部分路径拥塞、丢包、不稳定，表现为「偶发超时」而非恒定慢 |
| **合规与内容策略** | 机房所在地法律、云厂商区域政策、备案要求（若适用） |
| **回源** | 边缘在近处、源站在远处时，动态请求仍可能绕远 |

「香港节点」在工程讨论里常表示：**相对内地用户延迟可接受、又便于对接国际链路的一跳中转或源站位置**——是拓扑与产品选择，不是魔法加速开关。

选型直觉：静态内容优先 CDN 边缘；动态 API 看用户分布选源站区域，再决定要不要加一层边缘代理。

---

## 4. 路径过滤（「墙」）——只谈可观察症状

跨境与内容治理会导致**部分目的地或协议路径不可达或质量下降**。学习网络时，把它当成一类**中间盒 / 策略路由现象**：

| 现象（用户侧） | 可能落在哪一层（排查方向） |
|----------------|----------------------------|
| 域名解析到错误 IP、或与权威不一致 | DNS 污染 / 错误缓存 / 错误解析器 |
| 能解析但不能建立 TCP | 端口/路径被丢弃或重置 |
| TLS 握手中途失败、证书异常 | 中间干扰或指到了假站（需谨慎验证） |
| 部分站点极慢、丢包，其它正常 | 特定前缀/ASN 路径质量差 |
| 仅移动网络或仅某运营商不行 | 运营商出口策略差异 |

与本课其它块的咬合：

- **DNS/HTTPS**：先分清是「名字错了」还是「路不通 / 加密失败」  
- **正向代理**：客户端改出口路径（见下节与番外 Clash）  
- **合规**：了解现象是为了运维与排障；部署与访问须合法合规  

---

## 5. 正向代理、跳板与「节点」

| | 靠近谁 | 谁配置 | 典型用途 |
|--|--------|--------|----------|
| **反向代理 / CDN** | 服务器 | 站点方 | 入口、TLS、缓存、防护 |
| **正向代理** | 客户端 | 用户/企业 | 统一出口、审计、访问控制 |
| **跳板 / 代理节点** | 路径中途 | 由代理服务提供 | 流量经另一出口再访目标 |

\`\`\`
应用 →（可选）本机正向代理 → 远端节点出口 → 目标站
目标站 ← 反代/CDN ← 用户（另一条故事线）
\`\`\`

番外 **Clash** 讲的是本机引擎如何按规则选 DIRECT / PROXY；本课强调：**选路改变的是你的出网路径与出口 IP**，与源站前面的 Cloudflare 不是同一侧的设备。

企业场景还有：办公网强制 HTTP/SOCKS 代理、零信任客户端等——本质仍是「流量先到策略点，再出网」。

---

## 6. 上线排查清单（实务）

1. **DNS**：权威记录是否指向预期（CF 代理 IP 还是源站 IP）？TTL、缓存是否过期？  
2. **TLS**：证书域名是否匹配？是边缘证还是源站证？回源是否要求 HTTPS？  
3. **源站**：安全组 / 防火墙是否只放行边缘回源 IP？端口转发是否多余暴露？  
4. **出口 IP**：发信、第三方 API 白名单是否要固定 IP 池？  
5. **地域**：慢是全球都慢，还是仅某地区？用多地探测区分「源站性能」与「路径问题」。  
6. **代理**：本机是否误开系统代理导致开发请求走了意外出口？

---

## 7. 八股 × 业务串联

| 常考词 | 一句话 | 业务里长什么样 |
|--------|--------|----------------|
| **CDN 命中率** | 边缘直接返回的比例 | 静态资源缓存策略；命中低则回源贵、源站热 |
| **回源 / 回源 Host** | 未命中打到哪、Host 怎么写 | 配错 Host → 源站 404；防盗链 |
| **源站隐藏** | 对外只暴露边缘 IP | 安全组只放行 CDN/WAF 回源段 |
| **Anycast 入口** | 全球同一 VIP 感 | 「解析到的 IP 很怪但仍能开站」 |
| **出口 IP 白名单** | 对端只认你的固定出口 | 支付回调、开放平台、邮件投递 |
| **就近接入 / 跨区延迟** | 用户到 PoP、PoP 到源 | 静态走边缘；动态 API 仍吃源站 RTT |
| **企业正向代理** | 办公网统一出网与审计 | 开发机「公司代理」；与 Clash 同侧不同产品 |

---

## 与前后文

| 已学 | 本课落点 |
|------|----------|
| 路由 / NAT / 防火墙 | IP 池、入站限制、回源白名单 |
| DNS / HTTPS | CF DNS、污染症状、边缘 TLS |
| 反向代理 / CDN | 橙云 = 托管型全球反代 + CDN |
| 番外 Clash | 客户端正向代理与规则选路 |

## 下一步

第四章 **HTTP 与 www**（本仓入口挂载）；需要客户端选路细节时看番外 **代理引擎**。
`;

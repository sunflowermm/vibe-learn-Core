/** 系统的本质 */
export default `# 系统的本质

> 操作系统是 **资源与抽象的立法者**：谁使用 CPU、内存、磁盘与网卡，均经其调度与鉴权。  
> 本课建立 OS 核心模型，供终端报错与网络端口问题倒推。

## 本课分块

| 块 | 目标 |
|----|------|
| **为何需要 OS** | 多程序并存时的协调问题 |
| **四大抽象** | 进程、虚拟内存、文件、套接字 |
| **进程与线程** | 执行流的两层粒度 |
| **权限模型** | 用户、组、权限位与常见报错 |
| **内核 / 用户态** | 特权边界与系统调用 |
| **现象对照** | 后文故障如何落回本课 |

---

## 1. 为何需要操作系统

无 OS 时，程序需自行驱动设备、互斥占用 CPU、防止踩踏内存。多程序并存会失控。  
OS 的首要职责：**将硬件收敛为规则，再将规则借给全部应用。**

\`\`\`mermaid
flowchart TB
  subgraph apps[用户态 · 应用]
    A[浏览器]
    B[Shell]
    C[Node 进程]
  end
  subgraph kernel[内核]
    S[调度]
    M[内存]
    F[文件]
    N[网络栈]
  end
  subgraph hw[硬件]
    CPU[CPU]
    RAM[内存]
    DISK[磁盘]
    NIC[网卡]
  end
  apps --> kernel --> hw
\`\`\`

---

## 2. 四大抽象

| 抽象 | 程序视角 | 内核协调内容 |
|------|----------|----------------|
| **进程 / 线程** | 一条或多条执行流 | 时间片调度、上下文切换 |
| **虚拟内存** | 近似独占的地址空间 | 页表映射至物理内存 / 交换区 |
| **文件** | 路径树上的字节流 | 文件系统、块设备、权限检查 |
| **套接字** | 通向对端的通信端点 | 协议栈、网卡驱动 |

<details>
<summary>打开文件的大致路径</summary>

1. 应用发起 \`open\` / \`fopen\`（最终进入系统调用）  
2. 陷入内核，校验身份与路径权限  
3. 返回 **文件描述符（fd）**  
4. 后续 \`read\` / \`write\` 均携带该 fd 由内核代劳  

</details>

---

## 3. 进程与线程

| 概念 | 含义 | 工程直觉 |
|------|------|----------|
| **进程** | 资源分配的基本单位（独立地址空间） | \`node app\` 通常对应一个主进程 |
| **线程** | 进程内的执行流，共享多数资源 | 同一进程内可并发处理多请求 |
| **并发** | 宏观上「同时推进」 | 单核也可靠时间片交错 |

排障口诀：端口占用、内存爆炸、崩溃隔离——先问 **是哪个进程**；CPU 打满但内存不大——再看 **线程是否空转**。

---

## 4. 权限模型

| 概念 | 说明 |
|------|------|
| **用户 / 组** | 身份与批量授权单位 |
| **权限位** | 读 / 写 / 执行（文件与目录含义不同） |
| **提权** | \`sudo\` 等临时获得更高权限（慎用） |

| 现象 | 常见含义 |
|------|----------|
| Permission denied | 当前身份无权读/写/执行该路径 |
| Operation not permitted | 内核策略拒绝（权限、沙箱、保护） |

第一章终端与 PATH 问题，多数可回溯到「身份 + 权限位 + 路径是否存在」。

---

## 5. 内核与用户态

- **内核**：可直接操作硬件与关键表的特权环境  
- **用户态**：普通应用；通过 **系统调用** 请求内核代劳  

\`\`\`mermaid
sequenceDiagram
  participant U as 用户
  participant Sh as Shell
  participant OS as 内核
  participant HW as 硬件
  U->>Sh: 输入命令
  Sh->>OS: fork / exec 等
  OS->>HW: 调度与设备访问
  HW-->>OS: 完成
  OS-->>Sh: 退出码 / 输出
\`\`\`

---

## 6. 现象对照

| 现象 | 底层含义 |
|------|----------|
| Permission denied | 权限或身份不足 |
| Address already in use | 端口已被其他进程占用 |
| No space left | 文件系统或配额耗尽 |
| OOM / 被杀进程 | 内存压力与调度策略 |

## 下一步

同章：**软硬件联动**、**处理单元与存储**；  
出章：**不同终端环境**（向 OS 发令）。
`;

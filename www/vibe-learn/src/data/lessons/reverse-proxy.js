/** 反向代理与限流 — 对齐课件第十二章 */
export default `# 反向代理与限流

> **反向代理（Reverse Proxy）** 部署在**服务器一侧**：客户端以为自己在访问「网站」；实际由代理收请求，再转给后面的真实服务，并把响应原路返回。**客户端通常不知道真正的后端是谁。**

## 正向代理 vs 反向代理

| | 正向代理 Forward | 反向代理 Reverse |
|--|------------------|------------------|
| 位置感 | 更靠近客户端 | 更靠近服务器 |
| 谁知道代理 | 客户端常主动配置/感知 | 客户端常不感知后端 |
| 典型用途 | 访问控制、缓存、穿越限制 | 负载均衡、TLS 终止、缓存、防护 |
| 例子 | 浏览器代理、部分 VPN/企业代理 | Nginx、HAProxy、CDN 边缘 |

> 面试记忆：反向代理 = 服务器的「门面」，对外一张脸，对内可挂多台真身。

## 反向代理能干什么

1. **负载均衡**：请求分到多台后端（轮询、加权、IP 哈希等）  
2. **TLS/SSL 终止**：在入口做 HTTPS 加解密，后端可走内网明文或再加密，证书集中管理  
3. **缓存**：静态资源 / 部分响应缓存在代理，减轻源站  
4. **安全**：隐藏源站 IP；可配合 WAF、IP 限制、抗刷  
5. **压缩**：Gzip / Brotli 等  
6. **健康检查**：后端挂了自动摘除  

### 工作流程（一张图的文字版）

\`\`\`
客户端 → 反向代理（如 Nginx） → 后端 1
                          ↘ → 后端 2
                          ↘ → 后端 3
\`\`\`

浏览器只认识代理对外的域名和端口；后面几台机器怎么扩容，对用户透明。

### 和「开发代理 / CORS」的关系

前端开发时「同源代理到后端 API」，本质也常是**反代思路**：浏览器只打同域，由代理转发到真正的 API 端口，从而绕过浏览器跨域限制。

### 和状态码的关系

反代后面的上游挂了，你常看到 \`502 Bad Gateway\`、\`504 Gateway Timeout\`——问题不一定在「你写的业务代码」，也可能在网关与上游之间。

---

## 限流（Rate Limiting）

**限流**：控制单位时间内允许的请求量，防止打爆服务，也缓解恶意刷接口、突发流量。

### 常见算法直觉

| 算法 | 直觉 | 优缺点感 |
|------|------|----------|
| **固定窗口** | 每分钟最多 N 次 | 实现简单；窗口交界可能两波叠加 |
| **滑动窗口** | 最近一段时间合计 ≤ N | 更平滑；实现与内存成本更高 |
| **令牌桶** | 桶里定期加令牌，请求耗令牌 | 允许一定突发 |
| **漏桶** | 请求进桶，按固定速率流出 | 输出更匀，突发不友好 |

### 常见落地位置

- Nginx \`limit_req\` 等按 IP/路径限制  
- Redis 计数做分布式限流  
- 应用中间件（如 Express 限流）  
- API 网关统一限流  

> 记忆：反代管「怎么进门、分给谁」；限流管「进门速度别太疯」。两者常一起出现在网关层。
`;


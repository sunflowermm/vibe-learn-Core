/** 反向代理、限流、CDN */
export default `# 反向代理、限流与 CDN

> **反向代理**部署在服务器一侧：客户端访问统一入口，由代理转发至后端服务。  
> 常与 **负载均衡、TLS 终止、限流、CDN** 一起出现在工程入口层。

## 本课分块

| 块 | 目标 |
|----|------|
| **正向 vs 反向代理** | 位置与谁感知代理 |
| **反向代理能力** | LB、TLS、缓存、防护 |
| **CDN** | 边缘缓存与源站关系 |
| **Cloudflare 直觉** | 托管 DNS + 全球反代 |
| **限流** | 控制请求速率 |
| **八股 × 业务** | L4/L7 LB、会话保持、健康检查 |
| **与本仓** | www 挂载与网关直觉 |

---

## 1. 正向代理 vs 反向代理

| | 正向代理 | 反向代理 |
|--|----------|----------|
| 位置 | 靠近客户端 | 靠近服务器 |
| 客户端是否配置 | 常需配置 | 通常无感 |
| 用途 | 访问控制、出海、缓存 | 负载均衡、TLS、缓存、防护 |
| 例子 | 浏览器代理、Clash | Nginx、Caddy、云负载均衡 |

---

## 2. 反向代理常见能力

1. **负载均衡**：分发到多台后端  
2. **TLS 终止**：入口统一证书  
3. **缓存**：减轻源站  
4. **安全**：隐藏源站；可配合 WAF / IP 限制  
5. **健康检查**：摘除故障实例  

\`\`\`
客户端 → 反向代理 → 后端 1
                 ↘ 后端 2
\`\`\`

开发时「同源代理到 API」亦属反代思路，用于规避浏览器跨域限制。  
上游异常时常见 \`502\` / \`504\`。

---

## 3. CDN（内容分发网络）

**CDN**：把静态资源（及部分动态加速）缓存在靠近用户的边缘节点。

| 对比 | CDN 边缘 | 源站反向代理 |
|------|----------|--------------|
| 主要目标 | 就近命中、减延迟与带宽 | 应用入口、路由与防护 |
| 典型内容 | JS/CSS/图片/视频分片 | API、SSR、动态请求 |
| 未命中时 | 回源到源站（或中层） | 直接打后端 |

记忆：CDN 解决「拷贝放到用户旁边」；反代解决「门口如何接待与分发」。二者可叠加。

### Cloudflare 一类产品（预告）

把 **权威 DNS + 可选全球反代（橙云）+ CDN + WAF** 做成托管服务：访客先到边缘 PoP，再按需回源。  
细节（IP 池、地域、路径过滤症状）见下一课 **边缘与出口实务**。

---

## 4. 限流

控制单位时间请求量，保护后端并抑制滥用。

| 算法 | 直觉 |
|------|------|
| 固定窗口 | 每分钟最多 N 次 |
| 滑动窗口 | 最近一段时间合计 ≤ N |
| 令牌桶 | 允许一定突发 |
| 漏桶 | 匀速流出 |

落地：Nginx \`limit_req\`、Redis 计数、网关中间件等。

---

## 5. 八股 × 业务串联

| 常考词 | 一句话 | 业务里长什么样 |
|--------|--------|----------------|
| **四层 vs 七层负载均衡** | 看 IP/端口 vs 看 Host/URL/Cookie | NLB/LVS 偏 L4；Nginx/ALB/网关偏 L7 |
| **轮询 / 加权 / 最少连接** | 常见分发算法 | 机器性能不一时加权；长连接常用最少连接 |
| **会话保持（粘滞）** | 同一用户尽量打到同一后端 | 本地 Session 未共享时；更好方案是 Session 外置 |
| **健康检查** | 探活摘掉坏节点 | HTTP \`/health\` 或 TCP connect |
| **TLS 终止 / 透传** | 证书卸在门面 vs 直通后端 | 门面卸证省后端；透传则后端自己管证 |
| **蓝绿 / 金丝雀 / 灰度** | 流量切新版本策略 | 网关按比例、按 Header、按用户切流 |
| **回源 / 回源鉴权** | CDN 未命中打源站 | 防刷源、只允许边缘 IP 回源 |
| **令牌桶 / 漏桶** | 限流算法名 | 网关防刷、开放平台 QPS 配额 |

业务串联：用户 → CDN/LB → 反代 → 多实例；\`502\` 查上游与健康检查；登录态乱跳查粘滞或改 Redis Session。

---

## 6. 与本仓

主服将多个 Core 的 \`www/<应用>/\` 挂到同一入口，直觉接近「按路径分流的门面」。  
生产环境常在更外层再加云 LB / CDN / TLS。

## 下一步

**边缘与出口实务**（IP 池、Cloudflare、地域与代理）；第四章 **HTTP 与 www**；番外 **代理引擎**（正向代理侧对照）。
`;

/** 路由、网关、NAT — 对齐课件 */
export default `# 路由、网关与 NAT

> **路由（Routing）**：根据目标 IP 选择路径，把数据包从源主机转发到目标主机——网络层的核心能力之一。

## 知识串（接在哪）

| 已学 | 本课 |
|------|------|
| **IP / 子网** | 判断「同网段还是要找网关」 |
| **进程 / 端口** | NAT 常靠端口区分多台内网机 |
| 家里 Wi‑Fi | 路由器 = 默认网关 + 常做 NAT |

零基础场景：手机和电脑都能上网，但对外只显示「一个公网 IP」——背后就是 **NAT**。

## 包怎么走（宏观）

1. 源主机看目标 IP：是否在同一网络？  
2. 若否 → 交给**默认网关**（通常是路由器）  
3. 路由器查**路由表**，决定下一跳  
4. 可能经过多台路由器  
5. 到达目标网络，再交给目标主机  

\`\`\`mermaid
flowchart LR
  H[你家电脑] --> G[默认网关/路由器]
  G --> I[互联网上的路由器们]
  I --> S[目标服务器]
\`\`\`

### 路由表示例（直觉）

| 目标网络 | 掩码 | 下一跳 |
|----------|------|--------|
| 192.168.1.0 | 255.255.255.0 | 直接连接 |
| 10.0.0.0 | 255.0.0.0 | 192.168.1.1 |

「不知道怎么走」时，常有一条默认路由：丢给上游网关。

## 路由器本质是什么？

路由器是一台**带多个网络接口、跑路由软件的专用计算机**：

- **硬件**：CPU、内存、多个网口、转发相关部件  
- **软件**：路由协议（如 OSPF、BGP、RIP）、路由表、转发引擎、常带 NAT  

### 核心功能

- 选路、转发  
- 连接 / 隔离不同网络  
- 可做流量控制（QoS）等  

## 路由器 vs 交换机

| | 路由器 | 交换机 |
|--|--------|--------|
| 工作层次 | 网络层（L3） | 链路层（L2）为主 |
| 转发依据 | IP | MAC |
| 主要作用 | 连接不同网络 | 连接同一网内设备 |
| 广播域 | 通常隔离 | 默认不隔离 |

> 核心理解：路由器 = 多网卡计算机 + 路由软件，按 IP 转发。

## 默认网关

当目标不在本网段，主机把包先送给的那一跳，就是默认网关。  
家里常见：\`192.168.1.1\` / \`192.168.0.1\`（路由器的 LAN 口 IP）。

作用简述：

- 本网出入口  
- 帮你把包转发到其他网络 / 互联网  
- 常与 NAT 一起工作  

## NAT（网络地址转换）

**NAT** 把内网私有 IP 转成公网 IP（或一对多映射），缓解 IPv4 公网地址不足，并让多台内网设备共享出口。

### 家里最常见：PAT（端口级多对一）

\`\`\`
内网 192.168.1.10:5000  →  公网 203.0.113.1:12345
内网 192.168.1.11:5000  →  公网 203.0.113.1:12346
\`\`\`

外面只看见路由器的公网 IP；回程时路由器按端口映射还原给内网主机。

### NAT 类型对比

| 类型 | 特点 | 适用 |
|------|------|------|
| 静态 NAT | 一对一固定 | 内网服务器要固定公网映射 |
| 动态 NAT | 多对多，从地址池取 | 公网 IP 有限 |
| PAT / NAPT | 多对一靠端口区分 | 家庭 / 多数企业上网 |

### NAT 五步直觉

1. 内网主机发出：\`192.168.1.10:5000 → 公网服务器:80\`  
2. 路由器记录映射  
3. 改写成：\`公网IP:临时端口 → 服务器:80\`  
4. 发到互联网  
5. 响应回来再反向改写给内网主机  

### 实际场景

- 家庭：手机电脑电视共用一个公网出口  
- 企业：成百上千台内网机共享少量公网 IP  
- 云上：虚拟机常拿内网 IP，经 NAT/网关访问公网  

> 记忆：网关是「出口」；NAT 是「出门换工牌（地址/端口），回家再换回来」。

## 和后文 / 番外的关系

- **反向代理**：更靠近服务器门口；NAT 更靠近你家出口——别混侧。  
- **Clash 规则**：常按 IP/网段决定直连或代理，心里要有「路由表」的影子。  
`;

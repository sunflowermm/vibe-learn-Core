/** 路由、网关、NAT、防火墙 */
export default `# 路由、网关、NAT 与防火墙

> **路由**：按目标 IP 选择路径并转发数据包。  
> 家用场景常同时涉及：默认网关、NAT、以及简易防火墙规则。

## 本课分块

| 块 | 目标 |
|----|------|
| **包如何出网** | 同网段 vs 交网关 |
| **路由表直觉** | 下一跳怎么查 |
| **路由器 vs 交换机** | L3 vs L2 |
| **NAT / PAT** | 多台内网共享出口 |
| **IP 池直觉** | 动态地址从哪来 |
| **防火墙直觉** | 入站 / 出站过滤 |
| **端口转发** | 把公网端口映射到内网服务 |
| **八股 × 业务** | SNAT/DNAT、ACL、默认路由 |

---

## 1. 包怎么走

1. 源主机判断目标是否在同一网络  
2. 否 → 交给 **默认网关**  
3. 路由器查 **路由表** 决定下一跳  
4. 可能经多跳到达目标网络  

\`\`\`mermaid
flowchart LR
  H[内网主机] --> G[默认网关]
  G --> I[公网路径]
  I --> S[目标服务器]
\`\`\`

---

## 2. 路由器 vs 交换机

| | 路由器 | 交换机 |
|--|--------|--------|
| 层次 | 网络层（L3）为主 | 链路层（L2）为主 |
| 依据 | IP | MAC |
| 作用 | 连接不同网络 | 连接同一网内设备 |

---

## 3. NAT（网络地址转换）

将内网私有 IP 映射为出口公网地址（常见为 PAT：多对一靠端口区分）。

\`\`\`
内网 192.168.1.10:5000  →  公网 203.0.113.1:12345
内网 192.168.1.11:5000  →  公网 203.0.113.1:12346
\`\`\`

| 类型 | 特点 |
|------|------|
| 静态 NAT | 一对一固定映射 |
| 动态 NAT | 从地址池分配 |
| PAT / NAPT | 多对一；家庭最常见 |

### IP 池（地址池）

云与运营商场景里，常把一批公网地址放进**池**里按需分配：

| 用途 | 例子 |
|------|------|
| 动态 NAT | 出站连接临时占用池中地址 |
| 弹性 / 浮动 IP | 可在实例间迁移的「对外门牌」 |
| 负载均衡 / 代理出口 | 多入口或多出口共用一组地址 |
| 白名单业务 | 第三方 API 只放行你池中的固定出口 |

与后文 **边缘实务** 衔接：CDN/Cloudflare 对外往往是 Anycast 入口；源站仍可能绑弹性 IP，并用防火墙只允许边缘回源。

---

## 4. 防火墙直觉

**防火墙**：按规则允许或拒绝数据包通过（主机防火墙或网关防火墙）。

| 方向 | 典型问题 |
|------|----------|
| **出站** | 本机进程能否访问外网某端口 |
| **入站** | 外网能否主动连到本机监听端口 |

| 现象 | 排查方向 |
|------|----------|
| 浏览器能开网页，本地服务外网访问不到 | NAT 未做端口转发，或入站被拒 |
| \`Connection refused\` | 对端无进程监听，或被拒绝 |
| \`Timed out\` | 路径不通、丢弃或过滤 |

防火墙与 NAT 常同机配置：先有地址转换，再有「哪些端口允许进入」。

---

## 5. 端口转发（入门）

将路由器公网 IP 的某端口映射到内网主机端口，使外网可到达内网服务。

| 概念 | 说明 |
|------|------|
| 端口转发 | 网关把入站连接转到指定内网 IP:端口 |
| 与反向代理 | 转发偏「网络层/传输层入口」；反代偏「HTTP 应用层门面」 |
| 风险 | 暴露面增大；需配合防火墙与认证 |

企业里「开安全组 22/80/443」与家用端口转发是同一类问题：**允许谁从哪边进来**。

---

## 6. 八股 × 业务串联

| 常考词 | 一句话 | 业务里长什么样 |
|--------|--------|----------------|
| **默认路由 \`0.0.0.0/0\`** | 「其它都往这儿走」 | 主机出网、云 VPC 出公网网关 |
| **SNAT / DNAT** | 改源地址 / 改目的地址 | 出网共享公网 IP；入站把公网端口转到内网机 |
| **PAT / 端口复用** | 多内网共用一个公网 IP | 家用与多数云「出网 NAT」 |
| **ACL / 安全组** | 按五元组等放行或拒绝 | 只开 443/TCP；**MC 基岩要 19132/UDP**（协议写错=没开） |
| **端口转发 vs 反代** | L3/L4 映射 vs L7 门面 | 开发机临时暴露用转发；生产 API 多用 Nginx/网关 |
| **hairpin / 回环 NAT** | 内网用公网域名访问自己 | 内网访问自家域名偶发失败，需特殊处理 |

---

## 7. 与后文 / 番外

- **反向代理**：靠近服务器侧的应用层门面  
- **边缘实务**：IP 池产品化、回源白名单、地域出口  
- **Clash**：客户端侧代理；规则中的网段/IP 决策类似「选路」  
- **ICMP / ping**：网络层可达性探测（见协议栈补充）  

## 下一步

**DNS 与 HTTPS** · **HTTP** · **反向代理** · **边缘与出口实务**。
`;

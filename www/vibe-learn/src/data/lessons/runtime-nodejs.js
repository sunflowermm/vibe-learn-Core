/** Node.js 运行时 */
export default "# 运行时 · Node.js\n\n> **运行时（runtime）**：在操作系统里执行某类程序的引擎。  \n> **Node.js** 让 **JavaScript** 能在服务器 / 本机工具上跑，而不只在浏览器里。\n\n## 和前后课的关系\n\n| 已经学过 | 本课新增 | 下一课 |\n|----------|----------|--------|\n| OS 上的进程、系统调用 | 「执行 JS」的引擎叫什么 | 安装器如何把 `node` 放进 PATH |\n| 终端里敲命令 | `node -v` 查的是哪一套 | pnpm 如何按清单装依赖 |\n\n## 本课分块\n\n| 块 | 你要带走的 |\n|----|------------|\n| **浏览器 JS vs Node** | 同一种语言，两套运行时 |\n| **版本与 engines** | 为何本仓要求 Node ≥ 26 |\n| **和包管理器的边界** | Node 执行；pnpm 下载依赖 |\n\n---\n\n## 1. 同一种语言，两套「发动机」\n\n```mermaid\nflowchart LR\n  JS[\"JavaScript 源码\"] --> B[\"浏览器 · 内置引擎如 V8\"]\n  JS --> N[\"Node.js · V8 + 系统 API\"]\n  B --> DOM[\"DOM / 网页\"]\n  N --> FS[\"文件 / 网络 / 进程\"]\n```\n\n| | 浏览器里的 JS | Node.js |\n|--|----------------|---------|\n| 引擎 | 常为 V8（Chrome 系） | 同样基于 **V8** |\n| 额外能力 | 操作网页 DOM | 读文件、开端口、起服务 |\n| 典型产物 | 前端页面 | 后端、CLI 工具、本仓库主服 |\n\n**V8**：Google 开源的 JS 引擎，负责把 JS 变成可执行的机器动作。  \n**Node.js ≈ V8 + 一套绑定操作系统的 API**（文件、网络、进程……）。\n\n---\n\n## 2. 版本为什么重要\n\nXRK-AGT 的 `package.json` 写明 `engines.node >= 26`：版本过低会直接不兼容语法与依赖。\n\n| 概念 | 含义 |\n|------|------|\n| **LTS / Current** | 长期支持线 vs 新特性线；工程常钉死主版本 |\n| **`engines` 字段** | 项目声明「我按这个版本写的」 |\n| **全局 `node`** | Shell 里能敲到的，才是当前 **PATH** 指向的那一套 |\n\n```bash\nnode -v\n```\n\n应不低于项目 README 要求（当前为 **≥ 26**）。若提示「不是内部命令」，先去 **安装器与 PATH**，不是重装系统。\n\n---\n\n## 3. 和包管理器的边界\n\n| 角色 | 职责 |\n|------|------|\n| **Node** | 执行代码 |\n| **pnpm / npm** | 按 `package.json` 下载并链接依赖 |\n\n缺 Node，pnpm 也无法真正跑项目脚本；有 Node 无 pnpm，则装不齐本仓依赖。\n\n## 安装入口（按平台）\n\n- **Windows**：官网 **.msi** 或 winget / scoop  \n- **Linux**：发行版仓库、NodeSource、或 nvm / fnm  \n- **macOS**：官网 pkg、Homebrew、或版本管理器  \n\n## 下一步\n\n**安装器与 PATH** → 让 Shell 找得到 `node` → **包管理器 · pnpm**。\n";

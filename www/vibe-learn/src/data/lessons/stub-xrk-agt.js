export default `# 第二章预留 · XRK-AGT

> 从 **API 与前后端** 伸出的下一章挂点——还不是正文，只是位置与意图。

## 为什么挂在这里

「前后端 / API」讲的是软件如何协作；XRK-AGT 是把协作落到**具体运行时**：Core、HTTP、配置、插件、控制台。  
本质没变（请求、响应、分层），场景从「通用 Web」换成「本仓库工程」。

## 计划收录（后续填充）

- AgentRuntime 与加载器在网络中的角色  
- \`core/*/http\` 与 HttpResponse  
- www 静态 / \`sign.json\` 反代（和第一章「反向代理」对照）  
- 子服、配置归属等实践路径  

当前可先把第一章走完；本章有内容时，stub 会换成真实节点树，边关系保持「从前后端长出」。
`;

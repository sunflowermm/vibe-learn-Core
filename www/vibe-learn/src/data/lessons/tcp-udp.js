/** TCP / UDP — 对齐课件 */
export default `# TCP 与 UDP

> 传输层负责**进程到进程**的通信：用**端口**区分同一主机上的不同程序，并选择可靠或尽力而为的传送方式。

## 知识串

| 上一层问题 | 本课回答 |
|------------|----------|
| **IP** 找到哪台电脑 | **端口**找到哪个程序 |
| 序章的 **套接字** | 传输层协议（TCP/UDP）常通过 socket API 使用 |
| 后面的 HTTP | 多数跑在 **TCP** 上；DNS 查询常用 **UDP** |

记忆钩：**IP 找主机，端口找进程；TCP 挂号信，UDP 明信片。**

## TCP（传输控制协议）

特点可以记四条：

| 特性 | 含义 |
|------|------|
| **可靠** | 尽量保证完整到达，丢了可重传 |
| **有序** | 按序号拼回正确顺序 |
| **面向连接** | 传数据前先建立连接 |
| **流量控制** | 避免发得比对方收得还快 |

### 三次握手（建立连接）

1. 客户端 → 服务器：\`SYN\`（我想连）  
2. 服务器 → 客户端：\`SYN-ACK\`（好，我也准备好了）  
3. 客户端 → 服务器：\`ACK\`（确认）  

之后才开始正常传数据。  
还有四次挥手用于断开，入门先记住：**可靠连接不是免费的，要先打招呼**。

### TCP 常见场景

网页（HTTP/HTTPS）、邮件（SMTP）、文件传输（FTP）、多数聊天与 API——**错一点都不舒服**的业务。

## UDP（用户数据报协议）

| 特性 | 含义 |
|------|------|
| **快** | 不先握手，直接发 |
| **无连接 / 无状态** | 不维护复杂连接状态 |
| **不可靠** | 可能丢、乱序，自己要在应用层决定怎么处理 |
| **低延迟倾向** | 适合实时场景 |

### UDP 常见场景

在线游戏、直播、VoIP、很多 DNS 查询——**晚一点不如丢一点**的业务。

## TCP vs UDP 对照

| | TCP | UDP |
|--|-----|-----|
| 连接 | 要握手 | 通常不要 |
| 可靠 | 强 | 弱（尽力而为） |
| 速度 / 延迟 | 相对重 | 相对轻 |
| 比喻 | 挂号信 | 明信片 |

## 端口再强调

| 词 | 含义 |
|----|------|
| **端口** | 0–65535 的数字隔间，区分同机不同进程 |
| **监听 listen** | 服务进程占用某端口等待连接（第一章跑起的服务就在听端口） |
| 常见约定 | \`80\` HTTP、\`443\` HTTPS、\`53\` DNS（习惯，不是物理插孔） |

## 下一步

**路由与 NAT**（出网关）· **DNS/HTTPS**（名字与加密）· **HTTP**（应用层说话）· 番外 **Clash 端口**（本机代理也在 listen）。
`;

/** TCP / UDP — 对齐课件 */
export default `# TCP 与 UDP

> 传输层负责**进程到进程**的通信：用**端口**区分同一主机上的不同程序，并选择可靠或尽力而为的传送方式。

## 知识串

| 上一层问题 | 本课回答 |
|------------|----------|
| **IP** 找到哪台电脑 | **端口**找到哪个程序 |
| 序章的 **套接字** | 传输层协议（TCP/UDP）常通过 socket API 使用 |
| 后面的 HTTP | 多数跑在 **TCP** 上；DNS 查询常用 **UDP** |

记忆钩：**IP 找主机，端口找进程；TCP 挂号信，UDP 明信片。**

## TCP（传输控制协议）

特点可以记四条：

| 特性 | 含义 |
|------|------|
| **可靠** | 尽量保证完整到达，丢了可重传 |
| **有序** | 按序号拼回正确顺序 |
| **面向连接** | 传数据前先建立连接 |
| **流量控制** | 避免发得比对方收得还快 |

### 三次握手（建立连接）

1. 客户端 → 服务器：\`SYN\`（我想连）  
2. 服务器 → 客户端：\`SYN-ACK\`（好，我也准备好了）  
3. 客户端 → 服务器：\`ACK\`（确认）  

之后才开始正常传数据。  
入门先记住：**可靠连接不是免费的，要先打招呼**。

### 四次挥手（断开，常考）

1. 主动方 → 被动方：\`FIN\`（我发完了）  
2. 被动方 → 主动方：\`ACK\`（知道了）  
3. 被动方 → 主动方：\`FIN\`（我也发完了）  
4. 主动方 → 被动方：\`ACK\`  

为何常说「四次」：TCP 半关闭——一方不再发送，另一方可能还有数据要发。  
**TIME_WAIT**：主动关闭方在最后 ACK 后短暂停留，吸收迟到包、保证对端可靠关闭；高并发短连接时端口占用是常见面试题。

### 可靠与「别发太猛」（名称级）

| 机制 | 直觉 |
|------|------|
| **序号 / 确认号** | 丢了能重传、乱了能重排 |
| **滑动窗口** | 未确认前能在途发多少 |
| **流量控制** | 接收方窗口告诉发送方「我吃得下多少」 |
| **拥塞控制** | 网络忙时主动降速（慢启动、拥塞避免等名常考） |

### TCP 常见场景

网页（HTTP/HTTPS）、邮件（SMTP）、文件传输（FTP）、多数聊天与 API——**错一点都不舒服**的业务。

## UDP（用户数据报协议）

| 特性 | 含义 |
|------|------|
| **快** | 不先握手，直接发 |
| **无连接 / 无状态** | 不维护复杂连接状态 |
| **不可靠** | 可能丢、乱序，自己要在应用层决定怎么处理 |
| **低延迟倾向** | 适合实时场景 |

### UDP 常见场景

在线游戏、直播、VoIP、很多 DNS 查询——**晚一点不如丢一点**的业务。  
现代 Web 还有 **QUIC / HTTP/3**：在 UDP 上自己做可靠与多路复用（名常考）。

### 实务：开 Minecraft 服务器时，安全组开错协议就连不上

云主机（阿里云 / AWS 安全组、腾讯云防火墙等）规则是 **协议 + 端口**，不是「只写一个数字」。

| 版本 | 默认端口 | 必须放行的协议 | 常见翻车 |
|------|----------|----------------|----------|
| **Java 版** | \`25565\`（可改） | **TCP** | 只开了 UDP，或只开了端口忘选 TCP |
| **基岩版 Bedrock** | \`19132\`（IPv4；IPv6 常 \`19133\`） | **UDP** | 只开了 TCP 25565，基岩客户端永远进不去 |
| **Geyser 等桥接** | Java TCP + 基岩 UDP 常同时要 | **两套都要** | Java 能进、手机基岩进不去 → 查 UDP 19132 |

\`\`\`
玩家 ──TCP:25565──► 云安全组放行 TCP 25565 ──► Java 服务进程
玩家 ──UDP:19132──► 云安全组放行 UDP 19132 ──► Bedrock / Geyser
\`\`\`

对照本课与 **路由 / NAT**：

1. 进程在听哪个端口、用 **TCP 还是 UDP**（\`ss -ulnp\` / \`netstat\`）  
2. 云 **安全组 / ACL** 是否按**同一协议**放行入站  
3. 家用还要 **端口转发** 到内网机（协议选错同样失败）  
4. 本机防火墙（Windows 防火墙、\`ufw\`）也要一致  

记忆：**端口数字对了不够，协议也必须对。** 网页开 \`443/TCP\`；基岩开服开 \`19132/UDP\`。

## TCP vs UDP 对照

| | TCP | UDP |
|--|-----|-----|
| 连接 | 要握手 | 通常不要 |
| 可靠 | 强 | 弱（尽力而为） |
| 速度 / 延迟 | 相对重 | 相对轻 |
| 比喻 | 挂号信 | 明信片 |

## 端口再强调

| 词 | 含义 |
|----|------|
| **端口** | 0–65535 的数字隔间，区分同机不同进程 |
| **监听 listen** | 服务进程占用某端口等待连接（环境章跑起的服务就在听端口） |
| 常见约定 | \`80\` HTTP、\`443\` HTTPS、\`53\` DNS（习惯，不是物理插孔） |
| **知名 / 注册 / 动态端口** | 0–1023 常留给系统服务；客户端源端口多为高位临时端口 |

---

## 八股 × 业务串联

| 常考词 | 一句话 | 业务里长什么样 |
|--------|--------|----------------|
| **三次握手 / 四次挥手** | 建连与拆连状态机 | 握手失败 → 连不上服务；挥手异常 → 连接泄漏感 |
| **TIME_WAIT** | 主动关闭方短暂占坑 | 短连接打满临时端口；可调内核或改成长连接/连接池 |
| **SYN 洪水** | 半开连接占资源 | 需 SYN Cookie、防火墙、抗 DDoS |
| **Keep-Alive** | 连接复用少握手 | HTTP/1.1 默认长连接；连接池省 RTT |
| **粘包 / 拆包** | TCP 字节流无消息边界 | 自定义协议要定长/分隔/长度头；HTTP 自带分帧 |
| **TCP vs UDP 选型** | 可靠 vs 低延迟 | 支付/订单走 TCP；直播/游戏/QUIC 常涉 UDP |
| **安全组协议** | 规则含 TCP/UDP/ICMP 等 | MC 基岩须 **UDP**；只开 TCP 等于没开 |
| **QUIC / HTTP/3** | 应用可靠跑在 UDP 上 | 弱网优化；抓包看到 UDP 443 不奇怪 |

## 下一步

**路由与 NAT**（出网关、安全组）· **DNS/HTTPS**（名字与加密）· **HTTP**（应用层说话）· 番外 **Clash 端口**（本机代理也在 listen）。
`;

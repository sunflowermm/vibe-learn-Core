/** 配置归属 */
export default `# 配置归属

> 配错地方，比写错代码更难查。  
> 记住二分法：**框架模板** vs **产品模板**。

## 知识串

| 词 | 含义 |
|----|------|
| **CommonConfig** | 带 schema 的统一配置机制（表单、校验、加载） |
| **默认模板** | 第一次运行时从哪里复制出可改的配置 |
| **运行时数据** | 真正被进程读到的那份（常在 \`data/\`） |
| **schema** | 「这份配置长什么样」的结构化描述 |

和第一章 **package.json 契约**同构：清单不对，后面全错。

## 两套家

| 类型 | 模板默认放哪 | 运行时数据 |
|------|----------------|------------|
| AGT 运行时 / LLM 工厂 / system 体系 | \`config/default_config/\` | 配置系统加载后的实例 |
| 独立产品 Core | \`core/<名>/default/\` | 常在 \`data/<产品>/\` |

独立 Core **不要**把业务 yaml 塞进 \`config/default_config/\`。  
Schema 写在对应 \`commonconfig/\`，与消费代码一起改。

## 和「包管理」的类比

- \`package.json\` 声明依赖契约  
- \`default/*.yaml\` + schema 声明**行为契约**  

乱改全局默认，等于在所有产品上偷偷改依赖版本。

## 实践检查

改一项配置前先问：这是框架能力还是某产品能力？  
答案决定你打开哪个目录——这就是「项目实践」的日常功力。
`;

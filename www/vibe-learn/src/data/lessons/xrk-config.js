/** 配置归属 */
export default `# 配置归属

> 配错地方，比写错代码更难查。  
> 记住二分法：**框架模板** vs **产品模板**。

## 零基础先问一句

> 「我要改的，是**整座商场的规矩**，还是**某一家店的规矩**？」

| 答案 | 去哪里 |
|------|--------|
| 整座商场（运行时、通用 LLM 工厂、system） | \`config/default_config/\` |
| 某一家店（独立产品 Core） | \`core/<名>/default/\` + \`data/<产品>/\` |

独立 Core **不要**把业务 yaml 塞进 \`config/default_config/\`——那等于给所有店铺偷偷改电压。

## 知识串

| 词 | 含义 |
|----|------|
| **CommonConfig** | 带 schema 的统一配置机制（表单、校验、加载） |
| **默认模板** | 第一次运行时从哪里复制出可改的配置 |
| **运行时数据** | 真正被进程读到的那份（常在 \`data/\`） |
| **schema** | 「这份配置长什么样」的结构化描述 |

和第一章 **package.json 契约**同构：清单不对，后面全错。

## 两套家（对照表）

| 类型 | 模板默认放哪 | 运行时数据 | schema 常在哪 |
|------|----------------|------------|----------------|
| AGT 运行时 / LLM 工厂 / system | \`config/default_config/\` | 配置系统加载后的实例 | \`core/system-Core/commonconfig/\` 等 |
| 独立产品 Core | \`core/<名>/default/\` | 常在 \`data/<产品>/\` | \`core/<名>/commonconfig/\` |

改一项配置时，理想情况是 **三处一起动**：模板、schema、真正读配置的代码。

## 和「包管理」的类比

| 依赖世界 | 配置世界 |
|----------|----------|
| \`package.json\` | \`default/*.yaml\` |
| 锁文件 | 运行时落地的真实配置 |
| 乱全局装包 | 乱改 \`config/default_config/\` |

## 实践检查清单

1. 这是框架能力还是某产品能力？  
2. 模板目录选对了吗？  
3. schema / 消费代码有没有同步？  

答得清这三问，就比「到处搜 yaml」靠谱得多。
`;

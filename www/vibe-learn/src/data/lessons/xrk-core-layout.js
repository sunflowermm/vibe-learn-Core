/** Core 放码 */
export default `# Core 放码位置

> 规则极短：**业务进 \`core/<名>/\`，基础设施留在 \`src/\`。**  
> 违反时，热加载、配置归属、多产品并存都会开始「打架」。

## 零基础类比

把仓库想成商场：

| 地方 | 类比 | 你该不该乱改 |
|------|------|----------------|
| \`src/\` | 商场基建（电梯、电路） | 产品开发者一般不改 |
| \`core/<名>-Core/\` | 一家店铺 | **你的业务主要改这里** |
| \`data/\` | 店铺里的库存账本 | 运行后产生的数据 |

**Core** 在这里不是「CPU 核心」，而是「一个业务能力包（目录）」。

## 知识串

| 词 | 含义 |
|----|------|
| **plugin** | 挂到 Runtime 上的插件（常响应消息/指令） |
| **http** | 对外 HTTP API |
| **www** | 静态前端；必须 \`www/<应用名>/\` 子目录 |
| **commonconfig** | 配置的 schema / 表单约定 |
| **Loader** | 扫描上述目录并挂上 Runtime 的机制 |
| **system-Core** | 框架示例外设；与独立产品 Core 分工不同 |

你正在阅读的学习站，通常就来自某个 Core 的 **www**。

## 目录约定（记骨架即可）

| 子目录 | 放什么 | 零基础何时会碰到 |
|--------|--------|------------------|
| \`plugin/\` | 指令与插件 | 做机器人/指令时 |
| \`http/\` | HTTP API | 做接口时 |
| \`workflow/\` | AI 工作流 | 做 Agent 流程时 |
| \`tasker/\` | 协议适配 | 接 QQ/其它通道时 |
| \`events/\` | 事件监听 | 响应系统事件时 |
| \`commonconfig/\` | 配置 Schema | 加配置项时 |
| \`www/<应用>/\` | 静态前端 | 像本页一样做学习站/控制台 |

## 导入习惯（避免踩坑）

- 无自己 \`package.json\` 的 Core：可用根包 \`#infrastructure/*\` 等别名  
- 有自己 \`package.json\` 的 Core：用相对路径指回根 \`src/\`（子包没有 \`#\` 别名）  

## 下一步

能力要通过 **HTTP** 或 **www** 暴露给外部 → **HTTP / www**；  
改配置 → **配置归属**。
`;

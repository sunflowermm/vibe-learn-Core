/** 首次跑通 XRK-AGT */
export default `# 首次跑通 · XRK-AGT

> 把工具链收成一条可执行路径，然后**把接力棒交给第四章（项目实践）**。  
> 细节以仓库根 README「快速开始」为准；这里钉 **分层检查点**。

## 本课分块

| 块 | 内容 |
|----|------|
| **标准路径** | 版本 → 工作区 → install → 启动 |
| **倒推表** | 症状对应哪一层 |
| **和第四章的衔接** | 起跑线 vs 架构 |
| **可选分支** | Docker / 子服务（点到为止） |

---

## 1. 标准路径

\`\`\`mermaid
flowchart TB
  A[node -v 符合 engines] --> B[位于仓库根目录]
  B --> C[pnpm install]
  C --> D[node app / 文档入口]
  D --> E[打开控制台 / 健康检查]
\`\`\`

\`\`\`bash
# 1. 运行时（示例：已安装且 node -v ≥ 26）
node -v

# 2. 工作区（根目录需有 package.json）
cd XRK-AGT

# 3. 依赖（本仓只认 pnpm）
pnpm install

# 4. 启动（本地常见入口）
node app
# 或文档中的 Docker / 其他启动方式
\`\`\`

Redis 等基础设施：框架启动链会探测/尝试拉起；配置见仓库 \`docs/database.md\`。本课不展开运维细节。

---

## 2. 检查点（出问题时从这里倒推）

| 症状 | 先查哪一层 | 回哪张卡片 |
|------|------------|------------|
| \`node\` 不是命令 | PATH / 安装器 | 安装器与 PATH |
| 版本过低 | 运行时与 \`engines\` | 运行时 · Node.js |
| \`pnpm\` 不可用 | Corepack 或 pnpm 安装 | 包管理器 · pnpm |
| install 失败 | 网络、锁文件、是否在仓库根 | Git 工作区 / pnpm |
| clone 很慢或失败 | 远程主机与网络 | **代码托管** |
| 启动后打不开控制台 | 端口、防火墙、日志 | README 端口说明；网络章可对照 |

<details>
<summary>展开：一张「分层洋葱」</summary>

最外层是「网页打不开」，往里剥：

1. 进程在不在（启动日志）  
2. 端口监听了没有  
3. 依赖装全了没有  
4. Node 版本对不对  
5. 是不是在错误的目录里操作  

每一层对应第一章里的一张卡片，而不是随机重装系统。
</details>

---

## 3. 和第四章的衔接

| 第一章（本课） | 第四章 · 项目实践 |
|----------------|-------------------|
| 保证起跑线 | 讲 Runtime / Core / HTTP / 配置 |
| \`node app\` 能起来 | 代码放哪、配置如何归属 |

跑通之后，进入 **第四章 · XRK-AGT（项目实践）**，从「项目鸟瞰」开始。

## 4. 可选分支（本图不展开）

- **Docker Compose**：用容器打包运行时与依赖，本机少装工具链  
- **子服务（Python 等）**：主服可先独立跑；扩展能力再按文档启用  

先完成本机最小路径，再按需进入分支——这是「以小见大」在工程上的用法。
`;

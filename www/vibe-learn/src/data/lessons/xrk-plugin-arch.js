/** 插件式架构 */
export default `# 插件式架构

> 扩展方式为 **约定目录 + Loader 扫描 + 基类契约**（插件式 / 可插拔架构）。

## 知识串（融会前面章节）

| 已学 | 在本课的落点 |
|------|--------------|
| **进程 / OS**（序章） | 同一主服进程挂载多套扩展 |
| **模块 / 包**（语言·包管理） | 每个 Core 为可加载能力包；由 Loader 挂载 |
| **接口契约**（API 课） | 插件 / HTTP / 工作流继承基类，接口形状稳定 |
| **Core 放码** | 文件位于 \`plugin/\` \`http/\` \`workflow/\` 等约定目录方可被发现 |

\`\`\`mermaid
flowchart TB
  RT[AgentRuntime] --> LD[各类 Loader]
  LD --> P[plugin]
  LD --> H[http]
  LD --> W[workflow]
  LD --> T[tasker]
  LD --> E[events]
  LD --> C[commonconfig]
  P --> Core["core/<名>-Core/"]
  H --> Core
  W --> Core
\`\`\`

---

## 为什么要插件化（诞生原因）

| 痛点 | 说明 |
|------|------|
| **业务与基建耦死** | 每加一个机器人指令就改 \`src/\` → 无法多产品并存 |
| **热更新困难** | 内核与业务缠在一起，重载成本高 |
| **职责不清** | 「这段代码算框架还是算店铺？」说不清 |

插件式把问题改成：框架只提供 **插座（基类 + Loader）**；店铺（Core）自带电器。

---

## 它有什么作用

| 能力 | 说明 |
|------|------|
| **产品隔离** | 不同 Core 目录独立，互不侵入 |
| **统一挂载** | 均向 Runtime 注册，调试入口一致 |
| **约定发现** | 路径约定即注册，减少手工注册表 |
| **可替换** | 更换 Tasker / 工作流无需改动运行时内核 |

典型基类直觉：\`PluginBase\`、\`AiWorkflow\`、HTTP handler 约定、ConfigBase……  
细节见仓库 \`docs/base-classes.md\`、\`docs/框架可扩展性指南.md\`。

---

## 和「面向对象接口」的类比

语言课里的「实现接口」≈ 这里的「继承基类并放到对的目录」。  
**目录就是注册表；Loader 就是反射扫描。**

## 下一步

插件对外暴露 → **HTTP / www**；  
按语言生态实现专用能力 → **子服务端**（\`apis/<group>/\` 可落在各 runtime）；  
主服与子服语言对照 → **语言栈**；  
对话能力 → **Stream**。
`;
